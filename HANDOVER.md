# 交接台账 HANDOVER.md

> 机制说明见 `docs/superpowers/specs/2026-09-19-knowledge-html-generator-design.md` §10。
> 维护规则（三条，必须遵守）：
> 1. **一条工作项只在一处存在**：完成后从 TODO **删除**、追加进 COMPLETE（带日期 + 证据指针）；
> 2. **开发过程中冒出新需求 → 在 TODO 新增一条**（不要塞进已有项）；
> 3. **移除一个功能 → 删除对应工作项**，并在 `memo.md` 的决策记录里写为什么砍。
>
> IN PROGRESS 用于标记"当前正在做"，防止多会话重复劳动。

## IN PROGRESS（正在做）

- [ ] W-150 **新领域：Claude Opus 5.5 全面解读**（开发者 09-25 提）。要覆盖能力、费用、提升幅度、
      RSI（自我改进）等，落点是**这一版的差异化到底在哪、对后续模型路线的启示、以及对硬件的需求**。
      写之前要立的三条口径（不立就一定会写成宣传稿）：①**分数口径**——厂商自报与第三方复现分开列，
      同一 benchmark 的不同子集/不同推理预算不是同一个数，对比表必须标来源与日期；
      ②**费用口径**——每百万 token 单价、上下文长度、缓存/批处理折扣、限速窗口一起给，
      只给"便宜了 X%"是假比较；③**RSI 不许写成"AI 已经能自我复制"**：只报公开可核的
      自动化研究评测、人类监督点在哪、以及"哪一步仍然由人做"。
      硬件那一段要给**量级与推导口径**（参数量级、推理 FLOP、显存/带宽、功耗区间），
      明确哪些是厂商披露、哪些是公开估算；不预测股价、不给任何半导体标的的买卖建议。
      与已有的「大模型」篇重叠：那篇讲机制与格局，这篇讲一个版本节点的横切面，
      重叠处用引用而不是复述（大纲阶段就要划清）。


## TODO（待办）
- [ ] W-151 **新领域：美国中期选举与全球资产**（开发者 09-25 提）。要求：当前支持率大背景、
      两党各自手里的牌、不同选举结果对 A股/港股/美股/数字货币/黄金/石油 的传导。
      **这一篇的边界比前两篇更硬，因为政治与投资建议两条线同时贴近**：
      ①不预测谁赢——民调写清口径（抽样、时间窗、误差、Rasmussen/Temelra 与主流模型的
      方法差异本身就是内容），只给"若 X 则 Y 通过哪条机制"的**条件表**；
      ②不给任何标的的买卖建议、不给目标价，资产那一段只讲**传导机制与历史同类事件的读数口径**
      （关税、退税、财政停摆、政府关门、联储独立性质疑各自影响哪条链条），
      可证伪信号代替方向；③两党的"牌"要写成**制度性牌 vs 议程性牌**（参众规则、filibuster、
      预算调和、行政令可撤销性、法院任期），不是政策口号清单；④中国资产部分涉及监管与
      跨境数据口径，只报已公布规则与其生效时间，不做地缘政治推断。
      政治内容按红线 R-09 走：不劝投、不评价候选人、不写"某某阵营更好"。


- [ ] W-142 **`audit_images.py` 看不见"跑章"：主题级点名闸门在多主体领域形同虚设**。
      09-25 实测（大模型与加密货币篇）：`audit_images` 报 `✓ 配图 15 没点名 0 空章 0` 的同一份产物，
      人眼过联络表判出 **10/15 跑章**——稳定币章挂着 OpenAI logo、监管章挂着 Anthropic 融资图、
      仿真章挂着"OpenAI员工自曝996作息表"。根因不是引擎也不是采集器：`names_subject` 比的是
      **整份 aliases（18 条）**，而这一个领域横跨两个学科、有四个热点实体，
      "点名了领域"和"点名了这一章"是两件事——单主体领域（咖啡/香奈儿）里它们恰好重合，所以这个洞一直没露出来。
      **别把它做成闸门**：那会把 18 篇已上架页面大面积判死（同 W-97 的机制），
      先做成 `audit_images` 的一个独立维度（报数、不参与 `--fix` 的合格判定），
      拿 19 篇量一遍假阳性率再决定要不要升格。判据候选：图注/来源页须撞上**章题**的词，
      而不是撞上主题词——但"什么信号出现这篇就该改写"这类抽象章题会误伤，先量。
      **第二例证据同一天就有了，而且更极端**：`OpenAI` 这条词召回 35 条、**35 条全部点名 OpenAI**，内容全是融资、996 作息表、o3 发布、语音模式——整池 100% 命名、0% 相关。排除表救不了这种池子（要排除的是整条词而不是几个页面），第一章因此改画图。量到 0 相关只需一次 `reject_reason` 全池打印，不需要人眼——**这一维也许可以机器判**，W-142 要量的假阳性率应该往"图注撞上章题的词"这个方向试。
      本轮的修法是在**config 层**：把每章检索词从"领域里最热的实体"改回"这一章真正在讲的东西"
      （W-139 里我自己写错了，见该条 COMPLETE），机制章改画示意图。
      **09-25 量完了，两条词面判据都不成立，这条工单不该按原样做**：①判据"图注撞上章题里不属于 aliases 的词"→ 全站 332 张实拍里判跑章 **325 张（98%）**，纯噪声。原因不是实现坏，是**章题与照片之间没有词面桥**：本项目章题是论点句（"一条链看清楚：靶点到上市后，AI 现在真实渗透在哪几段"），照片只给实体名（"晶泰科技与礼来签署合作"），两者本就不该撞得上。②判据"同一条检索词挂在 ≥1/3 章节"→ 会误伤 MLCC（`贴片电容×11`）、积家（`积家手表×9`）、AI制药（`药明康德×6`）——单产品/单主体领域里全章复用同一个词是**对的**。剩下的处置：把"复用最高的词 × 几章"做成 `audit_images` 的一行**事实展示**（不判好坏、不参与 `--fix`），它当时就能让我看见自己把 `OpenAI` 挂了 5 章；语义那一维仍然只有人眼，AGENTS.md 第 6 步那句话是对的，别试图给它造一个代理指标。





- [ ] W-143 **摘一张图没有入口，只能手改三处**。`add_illustration.py` 的存在理由就是"一张图活在
      文件 / `data.json` / `manifest.json` 三个地方，手改一次就迟早漏一处"（W-115 的教训），
      但**反向操作一直没有工具**：本轮人眼判掉一张挂在新闻页上的"法槌+硬币+放大镜+监管二字"概念插画，
      只能自己写脚本删引用、删 manifest 行、删文件，再往 `image_exclude_pages` 里补来源页——
      四件事漏任何一件都会以四种不同方式坏掉（破链 / 孤儿 / 复活 / 下次重做又抓回来）。
      W-130/W-133/W-138 各手摘过一次，这已经是第四例，不再是单例。
      要做的是一条 `drop_image.py <领域> <文件>`：三处一起删 + 自动把来源页写进排除表 + 删完立刻
      跑 `validate` 的那一章（防止把某章摘空，G-03 会静默把整篇踢出索引——09-24 真踩过）。



- [ ] **同一条根因还在 `measure-layout.mjs`（G-13 真身）**：`waitForFunction(...).catch(() => {})` 同样咽掉超时，只是它 `if (!im.complete || !im.naturalWidth) continue` 是**跳过**而非误判，所以症状是漏检不是误伤（漏检更阴，因为它永远绿）。今天只修了 `shot.mjs`。改它会让 16 篇重新暴露，单独跑全站，不夹带。

（另**保留一条自我更正**：我先前用「raw 检索词里有没有主语 token」扫 Muse产业逻辑，得出「第 6、11 章零点名、只能走放宽档」并据此开了 W-124。量错了对象——`gated_queries` 会给 ≥3 词未点名的检索词补一条兄弟词，而契约测试量的正是补完之后的输出，一直全绿。那两章不是结构性没救，W-124 已删；检索词仍然换成了可拍摄实体，但理由要说准：抽象词本来就要不到图，而补出来的 `muse X Y` 会召回同名乐队与同名手术机器人。）

### 已知没修（等第二例证据再动，别单例过拟合）
- 同源单调：积家 11 张全部来自腕表之家系、娇兰多数来自花瓣图钉页。W-111 的来源页配额按 **URL** 数，同一站不同页判不出来；要做成闸门会大面积把已上架页面判死（W-97 那种机制），所以只在采集时生效。

## TODO（待办）

- [ ] W-153 **没有东西盯着「线上版主页过期」**。`dist/home/index.html` 是 gitignore 的中间产物，
      `record` 不重建它，`check` 与 G-14 也不读它。于是"20 篇都重发了、主页还链着旧指纹"
      这件事没有任何一处会报警——而主页恰恰是人唯一会点进去的那个链接。
      要做成：`check` 顺手把 `dist/home/index.html` 里的卡片 href 与账本 URL 对一遍
      （文件不存在就 warn，不 fail——它本来就要在发布前才构建）。
      review 09-25 提的，与 G-12 的 hosted 分支是同一个缺口的两面。

- [ ] W-154 **`review` 剩下的两处小账**：①`validate.published_pages(out_dir, topic)`
      收了 `topic` 参数却没用（现在靠目录结构恰好不错，语义上是骗人的签名）；
      ②`migrate_single_page.py` 用 `indent=2` 非原子写 `data.json`，正是
      `publish.py` 注释里警告过的那个 churn——迁移脚本自己犯了它要修的错。
      两处都是一行级修改，但会动到既有产物的字节，单独跑全站再合。

## COMPLETE（已完成）

| 日期 | 编号 | 工作项 | 证据 |
| 2026-09-25 | W-152 | **推到公开仓库 + 发布链路的 agent 化**。开发者拍板：远端只推一条全新的单提交（本地 124 条不动、作者中性），`output/*/images/` 的第三方图片不入库。落地 `scripts/publish_public.py` + `config/public_exclude.txt`：临时 index 造 tree，不碰本地 HEAD/index/历史；推前自查三件事（没有 output 图 / 没有 `.pending`、`manifest.json` 一类现场文件 / 没有本机用户名与绝对路径），外加一条**防自欺**：排除若一条都没删掉就直接报错。`tests/golden/**` 的 13 张（4.2 MB）是测试夹具，**刻意保留**并写明理由。同时把发布这半条链路交给 agent：`publish.py playbook` 算出每篇的 projectId 与逐步参数、`docs/publishing-for-agents.md` 手册、README 与 AGENTS 第 10 步指过去。深度 review 抓到并修掉 4 条 Important + 1 条 Critical（详见 W-152 原条目与 memo 09-25） | 公开仓库 <https://github.com/sherman9527/QoderWebSite> 的 `main` = 单提交 `3ea38d76ba641cf62ae3865e2f7d5671bdb5d6e4`，author `KnowEverything <snapshot@local.invalid>`、API 里 `login` 为空（不关联任何账号）、parents 为 0；`contents/output/%E5%92%96%E5%95%A1` 只有 `data.json` 与 `咖啡.html`，**没有 images 目录**；快照 611 → 205 个文件（删 406），自查 `图片残留 0｜中间文件残留 0｜身份痕迹 0`；本地 `python -m pytest` = 395 passed rc=0，`validate --all` = 20 领域 0 fail |
| 2026-09-25 | W-149 | **第 20 篇：名创优品 × 泡泡玛特**（开发者当天提的需求）。11 章、18 张图（5 张自画示意图 + 13 张实拍）、114 条来源。三条硬边界写在 angle 里并逐条验过：不给买卖建议/目标价/涨跌方向，「拐点」拆成经营、单店模型、估值三类各给可证伪观测条件；段永平一节只报披露记录与逐字原话，并给「跟随」在结构上不成立的四条（成本与时间尺度、仓位与流动性、披露滞后与选择性、他自己「看不懂就不做」的口径）；两家公司报表口径单列一章讲。**连带修掉一处模板级缺陷**：引用 chip 之间没有任何空白，`S0717S0720…` 排版上是一个不可断的词，挂十几条来源的标题被撑到 358px > 版心 345px，G-13 报 clipped-scroller。先试 `overflow-wrap` 会让编号从中间劈开（S071|7），正解是给每个 chip 前插 `<wbr/>` **造换行机会**；figcaption 同时补上漏掉的 overflow-wrap。模板指纹变了 → 20 篇全部重渲染重发 | 线上 <https://miniso-vs-popmart-gtqdc11h6po.qoder.website/>（curl 200、217 KB、18 图、11 章、114 来源锚点）；`validate --all` = **20 个领域 0 fail / 28 warn** rc=0；逐站 curl 全 200 且**线上 HTML 里 `<wbr` 计数 61–215**（证明修复真的发上去了，不只是本地绿）；边界正则扫描：方向/建议类 7 处命中全是页面自己的免责声明与来源标题原文，确定性判断 0 处、「就是拐点」0 处；`tests/test_render.py::test_source_chips_have_a_break_opportunity_between_them`；示意图 `scripts/diagrams/名创优品与泡泡玛特.py`（5 张，图上不写任何数字与日期，D-22）|
| 2026-09-25 | W-148 | **发布账本记住「这是哪个站」**（由我自己一次真实误操作逼出来）。`data.json.site` 新增必填 `project_id`，`record` 不传就拒绝写；`pending` 把工作清单带上 projectId，agent 更新时不必再凭记忆认站。顺带补上第二条自证不了的问题：`record` 现在校验 URL 主机名必须由该篇 slug 派生（`<slug>-…`）。我把 18 条 URL 拼成过 `https://<slug>/<后缀>`，`check` 照样报「账实一致」——账本和 data.json 由同一次错误生成，两处自洽不等于对 | 先红后绿：`test_record_rejects_a_url_that_is_not_that_slugs_address`（第一版红在 `DID NOT RAISE`，因为我留了 `host == slug` 这个口子，而那正是我打错的形式）、`test_site_record_carries_the_project_it_belonged_to`、`test_pending_tells_the_agent_which_project_to_update`、`test_the_data_contract_allows_the_published_site_block`。`validate --all` = 19 个领域 0 fail / 9 warn rc=0；18 篇全部 `published: true` + `active_release_id` 非空 + 公网 curl 200 |
| 2026-09-25 | W-147 | **主页双变体：一份模板出本地版与线上版**（D-23 拆出的第二条）。`build_index.collect/build(variant="local"|"hosted")`：本地版行为一字未变（链相对路径、G-12 继续 `os.path.isfile()`）；线上版卡片链各篇 `data.json.site.url`、封面**内联 base64**、输出到 `dist/home/index.html`。**比计划预估小一圈：模板一行没改**——卡片本来就是 `href={e.href}`、封面本来就是 `<img src={e.cover.file}>`，data URI 直接可用，所以不触发 AGENTS 第 5 步的全站重渲染。有一篇没发布时线上版**直接报错退出**（`NotPublished`）而不是悄悄退回相对路径：退回会得到一个"一篇跳公网、一篇跳本地文件"的主页，公网站上后者必然 404，而它看起来完全正常。线上版输出路径从 `output_dir` 的上一层推，测试指到 tmp 就不会写进真实仓库——不再多一个"要记得 patch 的常量"。**一处主动缩掉的范围（写进文档，不当成已完成）**：计划里说给 G-12 加一条 hosted 分支去校验线上版主页——没做。`validate.py` 全站段只看 `output/index.html`，要判 `dist/home/index.html` 得先给闸门加"校验哪个文件"的通路，而线上版当时根本产不出来（没有任何已发布地址）。那三条不变性现在**由测试守、不由闸门守**，这个区别文档里明写了。| 测试先红后绿：`tests/test_index.py` 四条（线上版链已发布站点且封面是 `data:` / 缺 URL 时 `NotPublished` / 线上版仍零运行时 JS 且保留排序控件 / **本地版即使全部发布了也不许改链公网**）。`python -m pytest` = **`382 passed in 352.47s`，rc=0**；`validate --all` = **`19 个领域，0 fail / 9 warn`**，rc=0；实跑 `build_index.py --variant hosted` 在无发布记录时 rc=1 并打 `✗ AI制药 还没有已发布地址，线上版主页不能出`（不接管道量的 rc——本仓库明写过 `| tail` 会洗掉真实退出码）。另：中文文件名小包（2,265,752 字节）验证 `succeeded`、`canPublish: true` → 上次 `sites_artifact_unsafe` 确认是体积 |
| 2026-09-25 | W-146 | **`scripts/publish.py` + `web_list.md` + G-14 账实核对**（发布这条链路的确定性半边）。**半自动是硬约束不是偷懒**：托管工具只有 agent 能调——`qodercli` 没有 sites 子命令、插件文档无 CI/REST，唯一能让纯脚本发布的路子是拿 PAT 逆向起那个 stdio MCP 服务，不做。所以脚本负责包/指纹/账本，agent 负责发布。接口：`pack <领域>`（逐字节复制，**不改名、不压图、不重写 src**；只装页面真引用到的图；排除 `manifest.json`/`search-stats.json`/`images.prev/`；超上限直接报错且不留半成品包）、`pending --json`（agent 工作清单）、`record <领域> <url>`（写 `data.json.site` 并重生成账本）、`check`。`web_list.md` 定为**派生物**、`data.json.site` 才是权威源——一份东西活在几处迟早不一致，这教训已付过两次学费（示意图三处、封面声明）；G-14 判"账本 ≠ 重算结果"，还没有任何发布时**保持沉默**（"还没有账本"和"账本错了"是两件事，把前者判死只会让人去关闸门）。**归因补上了**：上次 `sites_artifact_unsafe` 我同时改了体积和文件名、没法定责；这次单拿 2.3 MB + 中文图片名的包去 `prepare_site`，**通过并上传成功** → 原因是体积不是文件名，所以 `pack` 只报告非 ASCII 名字、绝不改名。指纹 = 包内每个文件字节的哈希汇总，**不含 `generated_date`**（它每次重渲染都变，拿它当指纹等于让 19 篇永远挂在待发布清单上）；`generate.py` 尾部只在指纹变化时 pack，否则 AGENTS 第 5 步"改一次模板 19 篇全部 `--offline`"会变成 19 次打包。`slug` 写进 `config/topics/*.json`（人写、不从中文名自动转写：地址一旦发布就改不掉，改名等于换 URL）。| 测试先红后绿：`tests/test_publish.py` 九条（逐字节相同 / 只带真引用的图 / 排除内部账本 / 超限报错且不留半成品 / 非 ASCII 只报告不改名 / record 之后 pending 变空 / 账本派生且手改会被发现 / 缺 slug 抛 `NoSlug`）、`tests/test_gates.py` 两条 G-14（不一致要响、没发布时沉默）、`tests/test_integration.py` 三条（绿页留包 / 内容未变不重复打包 / 闸门不过不留包）。`python -m pytest` = **`378 passed in 334.17s`，rc=0**；`validate --all` = **`19 个领域，0 fail / 9 warn`**；19 篇实出包 1.9–29.3 MB 全在 50 MiB 内；归因证据 actionId `3a30371a-03dc-49ff-850d-8f76ed345c5e`。另：D-23 声称"拆成 W-146/W-147"时我并没有真的建这两条——已补，W-147 在 TODO |
| 2026-09-25 | W-145 | **单页化：每篇只留最新版，产物名改成稳定的 `<领域>.html`**（开发者需求"每篇文章只保留最新版本 + 一篇一个站 + 主页兼容本地/线上"）。为什么不选"保留日期名、只留最新一个"：日期名让每篇深层链接每天变一次，`web_list.md` 里的 URL 跟着失效——**即使内容一字未改**；稳定名让"要不要重发"由内容指纹决定而不是由日历决定，版本历史交给 git。**这条动的是 W-103 非破坏式不变量的实现机制**（`created_of` 的注释原话："非破坏式不变量本来就要求旧日期页留着"），所以先立替代再删旧页：渲染到 `<领域>.html.pending`（扩展名故意不是 `.html`，对 `published_pages`/`_page_image_refs`/`_candidates` 全部不可见）→ 闸门判 pending → 通过才复制现役为 `.prev` 并 `os.replace` 换名 → 索引重建成功才删两份退路。`created_date` 落进 `data.json`（`generated_date` 每次 `--offline` 都变，W-128 早就避开它），缺字段时退回 `generated_date` **并喊一声**。G-01 反转：要求恰好 `<领域>.html` 且同目录不许有第二页——"只留最新版"靠闸门守，不靠约定。**实现被测试抓到两个我自己的 bug**：① G-01 第一版直接看候选文件的 basename，于是**每次正常发布都被自己判死**（它抱怨的那个名字恰恰是协议要求的样子）——闸门不能要求被检对象先变成它还没变成的东西；② 孤儿清理只放在闸门之后会**死锁**：LV 篇旧页一删、2 张图成孤儿 → G-03 不过 → 清理不跑 → 下次还是不过。现在两头各清一次，各挡一头（前防死锁，后防回滚页 `.prev` 一删那批图漏网）。还有一处计划没写到的坑：迁移删页后**索引从 19 静默掉到 14**（只被旧页引用的图立刻成孤儿 → 那几篇被 `_publishable` 跳过，而每一步退出码都是 0）——已让 `migrate_single_page.py --apply` 强制喊出下一步重渲染命令并配测试，这个坑不该靠人记得。| 测试先红：`tests/test_gates.py::test_G01_rejects_a_dated_page_name` / `..._rejects_a_second_page_in_the_same_topic_dir` / `..._judges_a_pending_candidate_by_its_resolved_name` / `test_a_topic_is_found_by_its_stable_page_name`、`tests/test_index.py::test_a_page_without_a_created_date_falls_back_and_says_so`、`tests/test_migrate_single_page.py` 六条（含"先回填再删"与 dry-run 不动盘）。`test_a_failed_gate_keeps_the_images_the_published_page_uses` 按换名协议重写并**加严**：现在同时断言"闸门没过则已发布页一个字节都没变"。`python -m pytest` = **`364 passed in 318.74s`，rc=0**；`python scripts/validate.py --all` = **`19 个领域，0 fail / 9 warn`**；索引 19 张卡、href 全为 `<领域>/<领域>.html`、0 个日期名；`migrate_single_page.py --apply` 实删 35 篇旧日期页 + 重渲染清掉 29 张孤儿图；单篇最大 LV 30 MB（仍在 50 MiB 内）。迁移顺序证据：`.probe/w145_rerender.txt`（首轮 5 篇 rc=1 全是那个死锁）→ 修完 `LV/浪琴/爱马仕/百达翡丽/积家` 五篇 rc=0 |
| 2026-09-25 | W-139 | **新领域：大模型与加密货币 上架**（开发者 09-24 需求：为什么大模型能破千禧年数学难题却没有破加密货币的新闻 / AI 股与 BTC 与稳定币概念股同时上涨怎么解释 / 底层原因 / 未来会不会被破解导致大幅下挫）。11 章 7362 字 / **12 张图（2 实拍 + 10 自画示意图）** / 封面声明 `03_椭圆曲线.jpg`。主题 `graphite-ultramarine`（石墨 #22252B + 群青 #1F3193；色板到 19 套已饱和，主色离最近只有 12.2，所以力气全放在强调色上：群青离所有现有强调色 53.0，网格搜出来的；四个文字色 AA 全过 16.6/5.6/14.1/10.0）；记忆点 `layer_cascade`＝无限层联立，那才是两个团队共同站在上面的东西。**题面前提查证后修正**：不是"AI 破解了千禧年难题"，是 2026-09-08 OpenAI 宣布其指导下约 10,000 个 agent 找到三维 Navier–Stokes 的**奇点存在性**结果、已用 Lean 形式化验证；争议在**优先权**不在对错（Buckmaster/Alpöge 早 12 小时，且写下"我读过最糟糕的 LLM 证明""只能叫 AI slop"）；策略真正属于 Córdoba 与 Martínez-Zoroa 的解析方法；Quanta 明写**没有直接实用后果**。一手来源只有 Quanta 可达（openai.com 403、SSRN/Guardian/TheConversation 不可达），凡引 OpenAI 的话都标明经由该报道转述。**全篇的钥匙是可验证性不是智能**：数学那边可信因为产物可机器复检；加密这边没有对应的检查器——唯一的检验是真的攻破，而攻破的瞬间全世界都能看到。所以"AI 在数学上快"与"AI 没攻破比特币"之间没有张力。**三条红线口径**：①只写机制、难度类别与迁移结构，不给任何可操作攻击路径、工具选型或实现细节（与 IPv4 那一章同判例）；②不下"某币安全/不安全"结论——`incumbents` 章画成"五条判据两栏问句"矩阵，每格都是问句、没有一格填答案；③同涨只给口径不做归因（三种解释分开摆，明写缺资金流数据所以不给方向），预测章只给四类可证伪信号、各对应推翻本篇哪一节。**配图这一篇把两道检查的盲区都撞出来了**：`audit_images` 报"15 张全合格"的同一份产物，肉眼判出 10 张跑章（W-142）；`OpenAI` 召回 35 条、35 条点名，内容全是融资与 996 作息表——**整池 100% 命名、0% 相关**，排除表救不了，第一章因此改画图。诚实检索词实测 22 条多词中文词形全部零点名，机制章没有一张照片承载得了论点，于是画了十张（验证链条、类别墙、迁移先后与两类暴露、三种解释、利差与那条不存在的边、监管三层、仿真边界、问句矩阵、可证伪轴、三方归属图）。| `python scripts/audit_images.py 大模型与加密货币` = `✓ 配图 12 没点名 0 空章 0 自画示意图 10`；`python scripts/validate.py --all` = **`19 个领域，0 fail / 9 warn`**（九条全是 G-05 偏长）；`python -m pytest` = **`353 passed in 448.12s`，rc=0**；联络表 `.probe/sheet_大模型与加密货币.png` 四轮逐张看（第一轮摘 10 张跑章、第二轮摘 1 张"法槌+硬币+放大镜"概念插画并写进 `image_exclude_pages`）；配图日志 `.probe/crypto_imgs{2..5}.log`（GBK）；示意图源 `scripts/diagrams/大模型与加密货币.py`；词形实测 `.probe/crypto_wordforms.txt` / `crypto_wf2.txt`；一手报道全文 `.probe/quanta_ns.txt` |
| 2026-09-25 | W-141 | **逐章回滚要问的是"这张图当初是哪条词找出来的"**。`_rollback_images` 原来的判据只有"这一章变少了没有"，于是**改检索词等于白改**：本轮把「稳定币」章的检索词从我写错的 `OpenAI`（领域里最热的实体，不是这一章讲的东西）换成 稳定币/储备/清算，重跑后该章 0 张合格，回滚按"变少了"把上一轮那张 `OpenAI放开ChatGPT限制` 原封退回来——跑章图原地复活，还造成两章共用一张，G-03 两条 fail。这也不是 W-99（不点名领域不退）能拦的：`names_subject` 比的是整份 aliases，OpenAI 当然"点名了这个领域"。**我在这条上翻了一次车，记下来免得下次再翻**：第一版判据写成"撞上 1 个当前检索词即可"，实测漏进两张（`学习软件编程` 里那个"软件"让一张椭圆曲线科普图回到 Lean 章），我抬到 2、还自我说服"复用放宽档那个常数不算新立阈值"——**全站 pytest 当场红了 `test_a_crashing_redo_cannot_take_the_page_down_with_it`**：点名档本来就允许 1 个命中，我那条复检比采集器自己还严，把"召回崩了要兜底"那条路一起堵死，某章饿到 0 图、闸门判死、索引不更新。真正的判据不需要阈值：旧记录带着 `query`，**只要它仍是这一章现在会发出的词**（子集＝拆词重试、同集＝颠倒词序、多一个主语词＝`ensure_subject` 的兄弟词）就是"这一章的旧图"；词已经不在了，它就是别的章的图。没有 `query` 的旧记录按"不知道"放行——不把"没证据"当成"证据是假的"（同 W-95：报"无来路"与报"可救"是两件事） | 测试先红：`tests/test_integration.py::test_rollback_skips_old_images_the_current_queries_would_reject`（三条断言：词没了的不退 / 词没变的不许退过头 / 补主语的兄弟词不算外章）。**这条测试同时把那个错误设计判了死刑**：≥2 那版是被 `..._cannot_take_the_page_down_with_it` 在全站套件里红出来的，不是被联络表看出来的——两条都要有。`python -m pytest tests/test_integration.py -q` rc=0；`scripts/images.py` 抽出 `text_hits()` 给 `_hit_count` 与体检共用 |
| 2026-09-25 | W-138 | **新领域：AI制药 上架**（开发者 09-24 需求十问：最优潜力的 A股/美股 / 产业链上下游 / Claude"随口发现新药"那件事 / 头部模型怎么走进药企 / 上市会加速吗 / 监管会有什么动作 / 实验猴与临床是必须的吗 / 会不会出现仿真平台 / 对传统药企的启示 / AI 理解创新药的底层逻辑）。11 章 **0 warn**（第一篇不触发 G-05 的）/ 20 张图（18 实拍 + 2 自画）/ 封面声明=`36_晶泰科技.jpg`（AI+实验机器人那张，1866×1280），主题 `indigo-coral`（靛蓝 #3D4E8C + 珊瑚橙 #C24E1E，实测主色距最近 18.4、强调色 26.2，G-11 要两道同时<25 才判双胞胎）。**题面那个前提是先被我自己误判、再查证纠正的**：我第一反应是"千禧年难题没被 AI 破过，你混了 Erdős 问题"，查了才发现 2026-09-08 Anthropic 官方页确实公布了 Claude 自主发现新酶系统——但**那是 ART（阵列关联逆转录酶），在噬菌体里，功能未知，不是药**；也不是"随口一句"（官方原话 All of the lab work is performed by human scientists，介入限于初始 prompt；BSL-1/2）。开发者转述的版本在两点上走了样，第一章的任务就是把这两点纠正回来。**全篇的承重墙是官方自己给的那串漏斗**：约 950 个 agent、21 小时、2.1 亿 token，20 万+ 条逆转录酶 → 3,500 个新候选 → 20 份人类可读报告 → **1 个值得上实验台（也可能一个都没有）**；配上 DiMasi 2016 的 25.58 亿美元与 rentosertib 的 NCT 时间线，得出**假设变便宜、验证一分没让、瓶颈被搬移并加剧**。这个论断一次性回答了"会不会加速/实验猴必须吗/会不会有仿真平台"三问：它们都不是"会不会"，是"压力先出现在哪一段"。**投资与预测按既有口径处理**：机型/标的走四级证据，三个预测问不给方向、只给决策因子与可证伪信号；医药题材特有的红线是**页面不得让读者以为"AI 已经能造药"**，所有 AI 成果都跟着"到哪一期、谁验证的、登记号是什么"一起写（zasocitinib III 期 NCT06671483、EXS74539 NCT07517198 等逐条带号；查不到期别的 DSP-0565 直接不列）。**配图第一轮 14 张里 13 张是 AlphaFold**——因为我声明了 `image_primary_subject: AlphaFold`，各章自己的中文词形全灭，于是整页退到同一个产品上。改成四个实测活的簇轮换（药明康德 35/33、晶泰科技 35/35、英矽智能 35/33、AlphaFold 35/35）后分布变成 6/5/5/5。再摘掉一张排行榜截图与两张重复的 XtalPi logo 卡。 | `python scripts/audit_images.py AI制药` = `✓ 配图 20 没点名 0 空章 0 自画示意图 2`；`python scripts/validate.py --all` = **`18 个领域，0 fail / 8 warn`**；`python -m pytest` = **`352 passed in 451.22s`，rc=0**；词形实测两批 32 条 `.probe/aip_wordforms.txt`/`aip_b2.txt`；联络表 `.probe/sheet_AI制药.png` 两轮逐张看；一手来源全文存 `.probe/anthropic_enzyme.txt`（curl 抓的，WebFetch 当时撞 API 限额 code 115）；示意图源 `scripts/diagrams/AI制药.py` |
| 2026-09-25 | W-140 | **主语闸门的一个系统性后门：两三个字母的 ASCII 缩写不该当主语名**。`names_subject` 对短 ASCII 词用"整词"匹配是**故意的**（LV 篇必须这样才收得下"LV Monogram 手袋"），但那个边界只挡拉丁字母与数字、**不挡中文**，于是 `ai` 命中"AI创作图…壁纸"与"…-ai格式-千图网"。AI制药 篇实测撞到：`实验室 自动化 液体处理` 与 `制药 生产线 车间` 报"点名"的候选全是千图网免抠素材与星空壁纸。继续查发现**这不是孤例**：AI眼镜 主语集有 `ai/ar1/jbd`、大模型 有 `tpu/glm`、Muse 有 `kvm/asn/nat/amd/arm/aws`（后两组是从 fact_prompts 混进 aliases 的基础设施缩写，是章节词不是主语）。加 `SUBJECT_STOP` 拦掉。**`llm`/`gpt` 故意不收**——对大模型篇它们确实是主语名，收了就是把闸门焊死。 | 测试先红：`tests/test_images.py::test_a_generic_infrastructure_abbreviation_is_never_a_subject_name`。修完自查了一件事：**18 个领域没有一个因此变成空主语集**——空集会让 `reject_reason` 的 `if subject and not names_subject(...)` 整条跳过，等于**静默取消点名要求**，那比漏洞本身更坏（`.probe/subj_after.txt`，最小的是咖啡 n=1，为既有形态且非空）。旁证：`NVIDIA BioNeMo` 的点名数从 24 掉到 0，那 24 条正是靠裸 `ai` 混过的 |
| 2026-09-25 | W-136 | **`bing_health.py` 现在分三个数量，不再把人的判断算成引擎的故障**。它原来数点名候选时 ①只看 `title`（而 `reject_reason` 看的是 title+purl+murl）②完全不读 `image_exclude_pages`。后果是实打实的：给 MLCC 加 7 条排除那一轮它照报"召回健康"，`collect()` 那边可用候选已经是 0，`specs` 最终空章、被 G-03 拦下不上架——**裁判放行了一次空章**。**修法上我推翻了自己第一版的方案**：本来想把"排除后可用"直接喂进健康度判据，写了测试才发现那会因为人挑得认真而把整篇判成不能跑（排除 18 条的领域永远不健康）。所以引擎健康度仍只看 `named`，排除表抽干可用是**另一条会单独喊的话**（`starved_by_exclusions`），既不隐瞒也不误杀。没有为此新增任何阈值。 | `tests/test_bing_health.py` 三条先红后绿：`..._reported_separately`（旧口径把 3 条全算成可用）、`..._flagged_even_when_the_engine_is_healthy`（旧代码根本没有这个信号）、`..._same_text_the_pipeline_uses`（只看 title 时 URL 点名的那条被漏计）；`verdict` 仍接受旧的四元组，`tests/test_audit_triage.py` 那 9 条健康探针测试一字未改全绿 |
| 2026-09-25 | W-133 | **新领域：骁龙笔电 上架**（开发者 09-24 需求：为什么笔电开始用骁龙 / 好处 / 生态 / 骁龙便宜吗 / 跑本地 agent 有优势吗 / 对笔记本发展的借鉴 / 最新机型举例）。11 章 7242 字 / **15 张图（10 实拍 + 5 自画示意图）** / 封面=Snapdragon X Platform 红色官方渲染（声明，1080×654）。主题 `snapdragon-obsidian`（曜石靛 #101A33 + 电光品红 #BD0598，靠强调色与现有 16 套拉开：品红离最近 57.7）。**这是第一篇机制章主要靠示意图撑起来的**：架构与功耗档、生态三层、两个价格口径、三约束短板、可证伪时间线——五章的照片全部"能过闸门但骗人"，硬配不如画。**四章的取证口径**：机型逐台带发布来源与日期且页尾声明"截至 2026-09"；价格章把「芯片口径」与「整机口径」显式分开、中间画出不相等的四段；本地 agent 章写成 NPU/带宽/功耗墙三约束的取舍而不是软文；compatibility 章保留那张 **i7 Surface Laptop Studio**——它是"x86 阵营在补能效"那一章的正证，不是跑题。**词形实测四批共 38 条的结论**：活池只有两簇（`Snapdragon X*` 34/34 与 `Surface Laptop` 25/25，且加后缀不改结果、是同一簇），其余全灭；`骁龙` 回动漫三视图、`骁龙 笔记本` 回福田欧曼油罐车，即 W-130 记过的整页兜底。**据此两次收紧 aliases：17 → 11**——删 `高通`/`Qualcomm`（`高通 总部 大楼` 点名 35/35 而头条是"高通 logo 设计含义"，点名率满分相关性零分）、删 `ThinkPad`（34/34 但回的是 x86 机型，与本篇论点冲突）、删 `Galaxy Book`/`苹果 M4`/`Lunar Lake`/`Strix Point`（竞品名当主语词＝把竞品照当本篇证据）、删裸 `骁龙`/`Snapdragon`（品牌覆盖的产品族远宽于"笔电"，手机/车载/掌机图全过）。**站级排除 18 条** | `python scripts/audit_images.py 骁龙笔电` = `✓ 配图 15 没点名 0 空章 0 自画示意图 5`；`python scripts/validate.py --all` = **`17 个领域，0 fail / 8 warn`**；`python -m pytest` = **`348 passed in 462.19s`，rc=0**；联络表 `.probe/sheet_骁龙笔电.png` 三轮逐张看（第一轮 22 张摘 18、第二轮摘 8、第三轮全过）；词形实测 `.probe/w133_wordforms.txt` / `w133_batch3.txt` / `w133_batch4.txt`；示意图源 `scripts/diagrams/骁龙笔电.py`（八张图全部通过"像素不许贴画布边缘"检查）；配图日志 `.probe/snap_redo3.utf8.txt` |
| 2026-09-25 | W-137 | **示意图的绘制脚本进 git**。四张 MLCC 示意图的 PNG 在 `output/`（进 git），画它们的脚本却在 `.probe/`（gitignored）——而脚本自己的注释写着"线条图是确定性的，同一份代码永远出同一张图，能进 git diff"。脚本不进版本库，这个理由就不成立。搬到 `scripts/diagrams/`，并把两篇共用的画图原语抽到 `_common.py`。**搬的时候踩到一个真缺陷**：`ROOT` 用两层 `dirname` 算，在 `.probe/` 下是对的，到 `scripts/diagrams/` 就变成 `scripts/`——而我第一次验证跑的是 `.probe/` 里的副本，等于没验。改成三层并**拿已装进产物的四张比 md5**，逐字节相同才算数 | `scripts/diagrams/{_common.py,MLCC.py,骁龙笔电.py}`；验证：`MLCC.py` 重跑产出的 4 张与 `output/MLCC/images/01_叠层剖面.png` 等 4 张 **md5 全等**；教训记进 memo 踩坑表（"验证一个搬动过的脚本，必须搬完之后在原位跑"） |
| 2026-09-24 | W-130 | **新领域：MLCC 上架**（开发者 09-24 需求：什么是 MLCC / 上下游材料 / 为什么当下能涨价、和 AI 的关系 / 国内外龙头 / 业绩能持续到什么时候）。11 章 6674 字 / 17 张图（13 实拍 + 4 自画示意图）/ 封面=`41_贴片电容.jpg` 尺寸阶梯全家福（**声明**，不走"最宽的一张"）。主题 `mlcc-ceramic`（烧结砖红 #9C3F27 + 镍电极钢蓝 #4E6B7A），记忆点 `interleaved_electrodes`＝叠层剖面本身。涨价章与国产章按 D-19 规矩：证据分级、每个价格数字带口径与 as_of、**不许预测价格**、结尾非投资建议声明。**三轮配图**：第一轮 22 张里 10 张是错件（`电容器` 这个上位词在 aliases 里，让电解/引线陶瓷电容全过闸门——删掉它之后 `audit_images` 独立报"没点名 8"，与肉眼一致）；第二轮 17 张里 7 张摘除（电解电容特写、电容器全家福、三张带卖点文字的 advertise 卡、一张 LED 投光灯）；第三轮摘掉一张**景德镇青花瓷瓶**。**站级排除共 12 条**：`hi1718.com` 与 `detail.1688.com` 整站拉黑，因为这一轮 4/4 张错图全出自这两站——1688 一条标着"陶瓷贴片电容0805B471K251"的 listing 配图是引线式瓷介电容，一条"贴片叠层电容1812"配图是 LED 投光灯，自家标题与自家图都对不上，没有逐张人眼之外的判据 | `python scripts/audit_images.py MLCC` = `✓ 配图 17 没点名 0 空章 0 自画示意图 4`；`python scripts/validate.py --all` = **`16 个领域，0 fail / 7 warn`**（七条全是 G-05 偏长，D-11/D-18 已容忍）；`python -m pytest -q` = **354 passed rc=0**；联络表 `.probe/sheet_MLCC.png` 逐张看过；配图日志 `.probe/mlcc_redo3.utf8.txt`；词形实测两轮稳定表 `.probe/w137d.txt`（`贴片电容` 35/33/排除后22、`钛酸钡` 35/34/13、`三环集团` 35/32/32、`风华高科` 35/25/25、`陶瓷 电容器` 34/13/11） |
| 2026-09-24 | W-135 | **`shot.mjs` 不许把「还没加载完」当成结论**。视觉基线自己红了两次、数字还不一样（`brokenImgs 0→4`、`0→6`），而量的那份冻结快照 12 张图全在磁盘上。两个独立毛病一起修：① `brokenImgs = !complete \|\| nw===0` 把"没加载完"与"加载失败"算成一件事 → 拆成 `brokenImgs`（只算 `complete && nw===0`）与 `pendingImgs`；② 等加载那句写成 `waitForFunction(...).catch(() => {})`，超时被咽掉，于是"没测准"与"没问题"打出同一份 JSON → 改成逐张 `scrollIntoView` 逼加载（大视口下步长 2232px 会跳过懒加载的距离判定，实测 @3762 滚完仍有 3 张没开始），收不住则 `process.exitCode = 1` 并说明原因。memo 里记过这条轴的另一端（LV 篇：`brokenImgs:0` 与肉眼白卡同时为真），同一个根：**在任意时刻采样 `complete`** | 测试先红：`tests/test_shot_settle.py::test_an_image_that_has_not_loaded_yet_is_not_called_broken`（实际 `brokenImgs:1`）与 `..._refuses_to_silently_settle_for_an_unsettled_page`（实际 `assert 0 != 0`）；第三条 `test_a_file_that_is_actually_missing_is_still_reported` 一开始就是绿的，钉住"拆没加载完不许顺手把真破图也拆没了"。诊断脚本 `.probe/imgdiag.mjs`（逐张报 complete/nw/loading）。修完 `pytest tests/test_shot_settle.py tests/test_visual_baseline.py -q` = 5 项 rc=0，基线**没重录**就回到绿——证明之前红的是测量不是模板 |
| 2026-09-24 | W-134 | **`--redo-images` 不许抹掉自画示意图**。`_clear_image_refs` 无差别把每章 `images` 清空，示意图也被清掉，随后渲染后的孤儿清理就把人画的四张图删了——"重做配图"与"抹掉自画图"是同一个动作。这是 D-19 那条非破坏不变量在示意图通道上的延续，W-132 开这个通道时漏了它。改成只清照片，`cleared` 也只数照片（日志要说真话） | 测试先红在 `assert [] == ['images/02_drawn.png']`：`tests/test_illustration.py::test_redoing_images_leaves_the_illustrations_alone`。MLCC 篇实测受益：第三轮重做之前装了四张示意图，`--redo-images` 跑完四张全在（`audit_images` 报 `自画示意图 4`） |
| 2026-09-24 | W-132 | **放行第四种图：自画示意图**（开发者 09-24 提"有些技术文档自己搭配生图更好理解"，并明确"已经做好的文章就不加了"）。有些章实拍答不上来——MLCC 的工艺链、叠层剖面、板级供电位置，`mlcc 产线`/`贴片电容 工厂` 全部 0 点名；以前只有两个结局：堆一张无关厂房照骗读者，或空章被 G-03 拦下。**按开发者裁决走"分 kind"而不是放松红线**：R-03 的可核查性没被放宽，是**换了载体**——示意图禁带 `source_page`（不可能冒充有出处），改为强制 `based_on`（追责到正文哪几条事实）。schema 用 `if/then/else` 让两种 kind 的必填项互斥；渲染打「示意图 · 非实拍」角标；索引与封面自动规则跳过它；`contact_sheet` 与 `audit_images` 单独计数控。图上刻意不写任何数字（层数/电压/容值/良率都是可核查量化断言，烧进图里就绕过 R-02）。入口唯一：`scripts/add_illustration.py` 同时写文件 + `data.json` + `manifest.json` | `tests/test_illustration.py` 6 项、`tests/test_render.py`（角标）、`tests/test_audit_images.py`（drawn 计数）、`tests/test_contract.py`（schema key）、`tests/test_redlines.py`（R-03 修订后的措辞）；契约见 `config/topic_data.schema.json` 的 `$defs/image.allOf[0].if` |
| 2026-09-24 | W-128 | **索引主页按创建时间排序（最新在前 / 最早在前）**。两点与开项时的预判不同，记下来免得下次照抄错的：① **没有新增字段**——创建时间取该领域**最早那篇日期页**的日期（文件名里本来就有，`generated_date` 每次 `--offline` 都会变成今天，用它排序等于"谁最近被重做过谁排最前"），所以不进大纲、不进 schema、不会漂移；② **不需要全站重渲染**——模板指纹的 `rev` 是常量、`blocks` 只跟区块枚举走，改 `IndexPage.tsx` 不动 15 篇专题页，只有 `index.html` 要重建。卡片左上角加日期角标（排序键必须看得见，否则读者只觉得顺序莫名其妙）；切换用纯 CSS（DOM 按创建时间升序，卡片带 `--asc/--desc` 两个序号，`body:has(#sort-old:checked)` 换 `order`），因为"产物零运行时 JS"是一条已有性质，有测试钉着 | 测试先红在 `KeyError: 'created'`：`tests/test_index.py::test_a_card_knows_the_day_it_was_created_not_the_day_it_was_rerendered`、`..._the_index_sorts_by_creation_date_both_ways_without_runtime_js`、`..._a_card_shows_its_creation_date`。**但单测全绿时功能其实是死的**——规则写成 `.feed:has(...)` 而 radio 不是它的后代，永远匹配不上；是 `.probe/sortprobe.mjs` 真开浏览器点了一次才发现（读 boundingRect 顺序，改前"完全倒序 false"、改后 true，15 张卡片两个方向都验）。视觉见 `.probe/index_sorted/index_1440.png` |
| 2026-09-24 | W-129 | **文档补齐：新建 `README.md`，并把两份旧文档的数字改成说真话**。此前没有 README——一个新人（或新会话）能读到的是 `AGENTS.md`（agent 冷启动）+ 一份 09-19 的设计文档（写着"待评审"、10 领域、10 条闸门），中间缺一份"这项目怎么跑起来、产物长什么样、为什么这么设计"。README 十节：现状与 15 篇分类表 / 前置与装依赖（镜像源是 R-07）/ 生成与校验命令 / 五阶段流水线 / **黄金模板**（React 唯一实现与三条备选方案的取舍、11 类区块、15 套主题 token 全表含主色与记忆点、封面"声明优先"、索引页排序为什么用纯 CSS、自包含）/ 内容从哪来（两趟取料与反幻觉契约）/ **配图怎么来**（三档阶梯、主语闸门、词形实测、品牌词按覆盖率自动选、排除表、召回跳水与健康闸门、最后一道是人眼）/ 13 条闸门各判什么与为什么存在 / 红线与八步流程 / 已知边界与代价 / 目录。设计文档**不回改正文**，加一节 §0「与 09-24 现状的差异」表——把设计文档改成永远正确，它就失去了时间戳的价值 | `README.md`；`docs/superpowers/specs/2026-09-19-knowledge-html-generator-design.md` §0；`AGENTS.md` 阅读顺序加 README 为第 0 步，并修掉三处过期数字（"当前生效红线为空，7 条候选等裁决"→ 9 条已生效；"10 类 block"→ 11；"10 套主题 × 2 = 20 产物"→ 15 × 2 = 30）。表里的数字逐个核过：`output/` 实测 50 个日期页 / 318 张图 / 约 160 MB，类别分布取自 `data.json` 实算，G-13 的七档视口取自 `measure-layout.mjs:WIDTHS` |
| 2026-09-24 | W-122 | **新领域：Muse产业逻辑 上架**（开发者 09-24 需求：产业逻辑 / 中国会不会出现这样的 app / 架构怎样 / 怎么看阿里云 AgentBay / 利好哪些硬件且必须区别于 AI 推理服务器）。11 章 7261 字 / 19 张配图 / 封面=润泽科技数据中心园区航拍（声明，不走"最宽的一张"）。**与 Muse 篇的分工**：那篇讲「是什么、怎么实现」，这篇讲「钱停在哪一层、中国凭什么、哪些硬件是执行层的生意」；投资章只写硬件且强制与推理服务器分开，证据分四级（订单公告 > 分部收入 > 互动平台表态 > 概念联想）。**上架前人眼摘掉 9 张**：Meta 字标、阿里云字标墙与 help 页头图、DeepSeek 鲸鱼 logo、腾讯云优惠入口卡、"Meta分析"讲义幻灯片（`q=Meta` 撞上统计学 meta-analysis）、kimi-thinking 标题卡、Luma 应用卡、1688 钢板网广告、两张同角度的润泽展位照——全部落成 `image_exclude_pages`（10 条站级前缀）。第一轮配图 19 张里 9 张是脏的，重做一轮 + 手工摘除后才干净 | `python scripts/audit_images.py Muse产业逻辑` = `✓ 配图 19 没点名 0 空章 0`；`python scripts/validate.py --all` = `15 个领域，0 fail / 6 warn`（六条全是 G-05 偏长，D-11/D-18 已容忍）；联络表 `.probe/sheet_Muse产业逻辑.png` 逐张看过；正文与几何见 `.probe/ind_page/Muse产业逻辑_2026-09-24_1440.png`（封面 1000×374、brokenImgs 0） |
| 2026-09-24 | W-126 | **品牌单词档不再搜裸品牌名：先搜这一章自己的两词短语，且全领域品牌词改由「本大纲检索词覆盖率」自动选**。`primary_subject` 原本只看形状（第一个够长或含中文的词），所以必然挑中领域名——13 篇恰好都对，`Muse` / `Muse产业逻辑` 不对：这两篇 22 条检索词里**一条都没写过裸 `muse`**，而 muse 在中文图搜里是英国乐队与一家同名手术机器人。判据是免费的（不联网、不加字段）：人写 `image_queries` 就是在赌「这个物体在图里会出现」。**只在领域名零覆盖时才换词**，实测 15 篇里只有 2 篇变（`muse→云电脑`、`muse产业逻辑→阿里云`），13 篇一个字不变。内部顺序也换了：本篇 19 张里裸词档带回来的 10 张摘掉 5 张，章节原词 16 张只摘掉 3 张——**裸词跑题率是章节词的 3 倍**，所以「这一章召不到图」时先把它自己的两词短语问一遍（`中际旭创 光模块 参数 对比`→`中际旭创 光模块`），两词是实测甜点位（三词召回崩塌，单词精度撑不住） | 测试先红在 `cov[subj.index(prim)] > 0`（`tests/test_images.py::test_the_fallback_word_is_the_one_the_outline_actually_searches`）与 `I.brand_phrase(...) is not defined`（`..._the_brand_tier_prefers_this_chapters_own_two_word_phrase`）；阶梯顺序由 `tests/test_integration.py::test_brand_word_is_tried_before_the_subject_gate_is_dropped` 钉住（新增"短语必须排在裸词前面"一条）。模拟全表见 `.probe/primary_analysis.txt`、`.probe/rule_sim.txt`、每章会搜什么见 `.probe/tier2_sim.txt`（本篇 10/11 章有章内短语）。`pytest -q` rc=0、`validate --all` 0 fail |
| 2026-09-24 | W-127 | **联络表的标签不再互相叠字**。`contact_sheet.py` 用 PIL 画标签而不折行，一条 52 字中文 alt 在 13px 下有 600+ px，一路画进右边两列，再被下一行的图压住——看 `sheet_Muse产业逻辑.png` 第一版时我完全读不出哪张是哪章，只能另写一张网格（`.probe/industry_grid.png`）才看得清。这一步现在是人眼验收的固定关卡（AGENTS 第 6 步），**表看不清等于没有验收**。改成按像素裁（`clip()`，不按字数——中文 13px 一个字 13px，拉丁词宽得多），三行标签：`[章节] 文件名` / alt / `q= src=`，文件名进标签是因为写排除表要点开它 | 测试先红在 `NameError: name 'ImageFont' is not defined` → 补断言 `font.getlength(clipped) <= TW-8`：`tests/test_contact_sheet.py::test_a_label_cannot_wider_than_its_cell_is_clipped`、`..._the_sheet_labels_two_lines_without_colliding`；重画后的效果对比 `.probe/sheet_Muse产业逻辑.png` |
| 2026-09-24 | W-125 | **大纲可以声明品牌单词档用哪个词**（`image_primary_subject`）。当天先按"人能比语料更懂"做了声明位，几小时后 W-126 用覆盖率把它自动化了，所以本篇**故意不声明**——留着当逃生口：自动规则挑错时人可以直接指定。只允许把本来就在主语集合里的词挪到第 0 位，不许新增词（新增等于给点名闸门开后门，让任意图进门）；声明不认识的词不生效 | `tests/test_images.py::test_a_domain_can_declare_the_brand_word_the_fallback_searches`（三种情形：生效 / 没声明时行为不变 / 声明野词不生效）+ `tests/test_contract.py::test_the_outline_contract_allows_declaring_the_image_primary_subject`（schema 声明了该字段，所以写进大纲不会被 G-10 判死） |
| 2026-09-24 | W-123 | **短语领域名不再被当成一个主语去补检索词**（同一根因的第二例，这次在采集侧）。W-114b 修的是探针侧「别拿 `Muse产业逻辑` 当检索词」，但 `subject_tokens` 里同一个 bug 还在：`TOKEN_SPLIT` 只按空白与标点切，不按中英切，所以领域名整体成一个 token 排在第 0 位，`primary_subject` 挑中的就是它——任何「没点名且 <3 词」的检索词会被拼成 `muse产业逻辑 深圳证券交易所 建筑`，图搜里搜不动，那一章点名档等于白跑。现在领域名再按拉丁串/非拉丁串分块（只切 topic，aliases 不动，别名本来就是人写的形态）。实测本篇 43 条检索词里受影响的是 1 条，但这个坑对每一个短语命名的领域都在 | 测试先红在 `短语整体不该当一个主语`：`tests/test_images.py::test_a_phrase_domain_name_is_split_before_it_reaches_a_query`（同时钉住纯拉丁名 `LV`、纯中文名 `百达翡丽` 不被拆坏）。改后实测 `primary_subject`：Muse产业逻辑 由 `muse产业逻辑` 变 `muse`，其余 6 篇不变（`pytest -q` rc=0，见 `memo.md` I-06） |
| 2026-09-24 | W-121 | **产物保留策略：决定保持现状，不删历史**（开发者授权「你来决定」）。实测现场：09-24 01:15 实测：`output/` 里 **49 个日期页 / 6.8 MB**、**283 张图 / 139.4 MB**，而且全部已进 git。增长不是泄漏，是 W-103 那条非破坏式不变量的必然后果——老日期页还引用着旧图，删图就会让已索引的页面破链，所以每一轮 `--redo-images` 只加不减（娇兰、积家、爱马仕各重做过 2–3 轮，一天就攒出 139 MB）。。**决定**：(a)(b)(c) 三条都不走——产物就是这个项目的交付物，`file://` 双击可开是需求的一部分，所以 `output/` 继续进 git；老日期页是「重做失败能退回上一版」的唯一凭据，删页会让只被它引用的图片变孤儿、反而诱发删图（W-103 那条不变量）。真正该管的是**别为「可能更好」重做已经合格的页面**：今天积家、娇兰各重做 3 轮、爱马仕 2 轮，体积就是这么攒出来的；W-120 的排除表落地之后，重做的必要性进一步下降。真要瘦身是 git 历史重写（`git filter-repo`），破坏性且不可逆，需要单独一次授权，不在日常里做 | `du` 实测 49 页 / 6.8 MB、283 图 / 139.4 MB；决定与三条路的取舍记在 `memo.md` 09-24 04:0x 一节；纪律落在 `AGENTS.md` 第 6 步（重做之后必须重看联络表） |
| 2026-09-24 | W-114b | **`--topic` 探针改成跨章取样，不再拿领域全名当检索词**。给 Muse产业逻辑 造探针时发现两个错法：① 用领域名 `muse产业逻辑` 当探针词——那是个短语，图搜里没有这种东西，必然零点名，gate 就会说「这篇不能跑」，而那是探针的错不是引擎的错；② 另外两条都取自第一章——那一章恰好词形不好就误判整篇，恰好好就其余十章的坑一个没探到。现在取「每章第一条点名领域的检索词」，凑不满才补一条短品牌词（≤4 字，短语领域名不用） | 测试先红在 `拿领域全名当探针，必然零点名`：`tests/test_audit_triage.py::test_the_topic_probe_spreads_across_chapters_not_one_chapter`；改后实测四篇：娇兰 → 三条 `Guerlain *` 拉丁形态、积家 → `积家手表` 打头、Muse产业逻辑 → 三条跨章。`pytest tests/test_audit_triage.py` 16 项绿 |
| 2026-09-23 | W-83 | **娇兰篇上架**。11 章 7042 字 / 22 张配图 / 空章 0 / 没点名 0。卡了一天半的仍是词形：`娇兰 香水`、`Orquidee Guerlain 1912`（品牌词不打头）0 点名，换成 `Guerlain + 任一拉丁词` 打头就 33/35——已把 11 章检索词第一位换成实测形态 | `python scripts/audit_images.py 娇兰` = `✓ 配图 22 没点名 0 空章 0`（`.probe/queue6.log` 18:51）；联络表 `.probe/sheet_娇兰.png` 逐张看过：Shalimar、Abeille Royale 蜂巢、L'Heure Bleue、Meteorites、蜜蜂瓶各归其章。**已知代价**：多数图带节庆促销文案（520 爱礼盛放、新年开门红、JD 角标），按 D-19 属「品牌正确、只是长得像广告」的保留类；封面位不是广告图 |
| 2026-09-24 | W-120 | **人眼结论现在能落成数据：大纲新增 `image_exclude_pages`**。动因是 09-23 实测：18:35 手工摘掉爱马仕那张橙色字标面板，22:40 重做时它被原样抓回来、只是换了个章节；Muse 上架当天联络表又抓到一张——「MUSE 手术操作演示亮相上海消化病学术大会」（musemedical.cn，一家同名手术机器人公司）被配在「几分钟拉起上万个沙箱」这一章，而 `audit` 报「没点名 0」，因为标题里确实有 muse。**评分与关键词否决都拦不住这类**（D-17 量过：按词否决要赔 27 张好图）。所以判据只按**来源页 URL 前缀**——不猜语义，误杀面是零；写成前缀而不是整条 URL，是因为一个只发宣传图的站每一页都是宣传图。排除排在来源页配额与下载之前，并且**逐章退回那条路径也拦**——退回不经过采集，不在这里拦一道，摘掉的错图会在下一次「变少就退回」时复活 | 测试先红在 `collect() got an unexpected keyword argument 'exclude_pages'`：`tests/test_images.py::test_a_source_page_a_human_excluded_stays_out_of_every_later_redo`、`..._excluding_a_site_prefix_excludes_its_pages_too`（后者钉前缀语义），契约侧 `tests/test_contract.py::test_the_outline_contract_carries_the_manual_image_exclusions`。**实数据验证**：`I.search(u'muse 大会')` 召回 20 条，其中 7 条命中排除表，全部在下载前丢弃（日志「丢弃（人工排除的来源页）」）。已给 Muse 2 条、爱马仕 1 条、LV 2 条 |
| 2026-09-23 | W-117 | **Muse 篇上架**（开发者 09-23 新增需求：Meta Muse 深度解析 + 云电脑产业链）。11 章 6349 字 / 16 张配图 / 空章 0 / 没点名 0，`validate --all` = **14 个领域 0 fail**，索引 14 张卡片。六个问题各自成章，另加三章补地基（为什么需要一台云电脑、中游格局、单位经济）。两处划线的地方按线交付：投资机会章明写「没有哪家公司写出 Muse 的订单或金额，Meta 也未公开算力供应商名单；涨幅属市场定价，不能当成已供货」并带非投资建议声明；IPv4 章只讲机制与三层合规（RPKI/ROA/IRR、Cloudflare verified_bot、LinkedIn 8.2 与 GitHub D.9 条款原文含生效日期），并写明「把机房 IP 伪装成住宅 IP 正是灰产做法，也是本节不展开的部分」。主题 `muse-sandbox` + 记忆点 `sandbox_rack`（三轮才调对：格数不够在 1440 封面上漂成三行、线性同余会让亮格排成对角线） | 配图两轮：queue9 等到 03:17 没等到窗口，03:21 手测 `muse` 35/35 点名后立即补全；**联络表抓到一张真错**——`images/11_muse_大会.jpg` 是另一家叫 MUSE 的手术机器人公司（musemedical.cn）被配在沙箱章，`audit` 因为标题含 muse 而放过，正是上架前记在台账里预判的同名词坑；另摘 Meta 字标图，封面从自动选中的 Ubuntu 字标 splash 改声明为 App Store 榜单截图（「Muse from Meta」列在 ChatGPT 之上，直接证「爆火」）。证据：`.probe/sheet_Muse.png`、`.probe/muse_cover/Muse_2026-09-24_1440.png`、`python scripts/validate.py --all`（03:3x，14 领域 0 fail）；过程记录见 `memo.md` 09-24 03:3x 一节 |
| 2026-09-23 | W-98 | **积家篇按 D-19 的判据上架（选 (c) 接受单图池）**。判据四条全中：配图 11 ≥8、11 章每章 ≥1、`没点名 0`、联络表人眼确认 11 张全是积家表（Reverso 方壳、Duomètre、陀飞轮框架、Polaris、鸟鸣三问、绿色玛瑙面 Reverso）。起效的还是那条词形杠杆——`积家手表`（中间不留空格）35 召回 / 35 点名 / 22 个来源页，而任何「积家 + 空格 + 词」都是 0。**接受已知代价**：11 章共用一个图池、全部来自腕表之家系，章节级相关性拿不到，这是引擎上限不是大纲问题。没有砍章（正文 9 章 6357 字是花过额度的） | `python scripts/audit_images.py 积家` = `✓ 配图 11 没点名 0 空章 0`；联络表 `.probe/sheet_积家.png`；`python scripts/validate.py --all` **rc=0，13 个领域 0 fail**——这是全站第一次全绿，索引从 11 张卡片涨到 **13 张** |
| 2026-09-23 | W-116 | **R-04 的判据盲区，决定走「人眼抽查」而不是加规则**（开发者授权「你来决定」）。三条路里：(c) 上 OCR/图像分类被否——为一个纯静态、零外部依赖的产物引一个模型依赖，代价与收益不成比例；(a) 纯接受也被否——封面已经因此出过两张被裁掉半截字的卡片（W-119 就是它的产物）。选 (b)：把「配图有变动的领域，上架前必须出联络表用眼睛过一遍」写进 `AGENTS.md` 干活流程第 6 步，紧挨着「闸门是唯一裁判」那句，并写明为什么闸门不够（常柴柴油机、木工坊、永乐大钟、西铁城机芯——四次都是闸门全绿）。存量那 5-6 张品牌正确、只是长得像广告的**正文图保留**：读者看到的品牌没错，而最显眼的封面位已经换成非广告图 | `AGENTS.md` 第 6 步（同文件里可查）；决策与三条路的取舍记在 `memo.md` D-19；清单与图证：`.probe/sheet_AI眼镜.png`、`.probe/sheet_浪琴.png`、`.probe/sheet_LV.png` |
|---|---|---|---|
| 2026-09-23 | W-119 | **封面要能声明**。默认规则是「取最宽的一张」，而这条规则天然偏爱排行榜截图与宣传横幅——它们像素宽度最大。索引截图实测两张卡片被 `object-fit:cover` 裁掉半截文字：大模型篇用的是 3564px 的排行榜（卡片上是一排看不懂的表格），AI 眼镜篇用的是 3840px 的光波导渲染图（标题被切成「OOKTECH」）。`build_index.cover_of` 早就写了「先看 data.cover」的分支，但契约的根 `additionalProperties:false` 不认这个键——**那段代码是死的**，而且我第一次是直接往 data.json 手写 cover，被 G-10 当场判死（两个领域的产物差点没上架）。现在把图片形状抽成 `$defs/image`，根上加可选 `cover` 引用它，模板 `coverOf` 同规则；两处必须一致，否则卡片点进去会换一张图 | 测试先红在 `契约不接受声明封面`（schema）与 `声明的封面没上页面首屏`（模板）：`tests/test_contract.py::test_a_declared_cover_is_accepted_by_the_contract`、`..._must_still_be_a_real_image_shape`（缺 alt/source_page 的封面仍被拒——声明封面不是第二个不受契约管的图片来源，R-03 照样管）、`tests/test_render.py::test_a_declared_cover_wins_over_the_widest_image`（同时断言索引与页面用的是同一张）。两张封面改完重截索引核对：大模型 → H100 晶圆裸片照（我先换成 NVIDIA 门头照，看图才发现那是品牌 logo，撞 R-04，又换了一次——**这一步只有看图能发现**）；AI 眼镜 → 雷鸟 Air Plus 产品照。`.probe/index_full3/index_1440.png` |
| 2026-09-23 | W-118 | **R-08 漏了：设了 cwd 也挡不住研究文件落进仓库根**。llm.py 早就把无头 CLI 的 cwd 设成 `.research-scratch/`（候选-H 落成的那条红线），但 CLI 找的是它自己眼里的「项目根」——从 cwd 往上走到有 `.git` 的那一层，于是抓到的网页还是写在仓库根。09-23 实测：AI 眼镜篇研究期间根目录多出一个 39KB 的 `cdt.html`（雷鸟 AR 眼镜文章），**是 git status 抓出来的，不是测试**。**归因要更正：这个机制不是今天发现的**——`memo.md` D-15（09-20）已经写下「子代理写绝对路径或 `../` 时 cwd 拦不住它，这不是 bug 能修掉的，必须假设会发生」，当时的处置是手工把散件挪进 `.probe/strays/`；今天做的只是把那份手工变成自动。——原有那条测试只断言了 cwd 参数等于 scratch，而那恰恰是被绕过的那一步。现在 `_run` 在调用前后各数一次根目录文件，把**本次新增的文件**搬进 `.research-scratch/escaped/` 并打一行说明；目录与既有文件一律不碰，搬不动就跳过（并发下另一个 worker 可能已经搬走了） | 测试先红在 `module 'llm' has no attribute 'ROOT'`，真判据是 `tests/test_llm.py::test_a_research_file_dropped_in_the_repo_root_is_pulled_into_scratch`（夹具里的 fake CLI 故意往仓库根写一个 cdt.html，断言它消失、出现在 escaped/、且既有的 AGENTS.md 没被搬走、日志说了文件名）；`pytest tests/test_llm.py` 29 项绿、全量 315 项 exit=0。现场文件已搬：`.research-scratch/escaped/cdt.html` |
| 2026-09-23 | W-107 | **AI 眼镜篇生成并上架**（开发者 09-22 深夜新增需求）。11 章 6757 字 / 22 张配图 / 空章 0 / 没点名 0，`validate` = **0 fail / 1 warn**（warn 是 G-05 正文偏长，按 D-18 属可接受），索引从 10 张卡片涨到 **11 张**。开发者点名的每一项都落到了章节：产品名（雷鸟 X3 Pro·Rokid·XREAL·小米·夸克·联想·华为·Meta Ray-Ban）、SOC 与多芯架构（高通 AR1 / 恒玄 BES2800 / 展锐）、电池与续航口径（镜腿 200mAh 怎么分）、镜框与电致变色与近视配镜、光机之争含扫描激光（MicroOLED·MicroLED·LCoS）、镜片与光波导、**A股/港股/美股逐环点名且区分「公告里有客户名或金额」与「互动平台表态」**、技术路线对照、隐私与生态边界。检索词全部点名具体产品而不是抽象概念——这是当天从大模型篇学到的那条 | `python scripts/audit_images.py AI眼镜` = `✓ 配图 22 没点名 0 空章 0`（`.probe/queue2.log` 14:10）；联络表 `python scripts/contact_sheet.py AI眼镜` → `.probe/sheet_AI眼镜.png`（22 张逐张看过：真机图、波导光线追迹图、拆解 BOM 图各归其章）；几何 `node scripts/shot.mjs output/AI眼镜/AI眼镜_2026-09-23.html --widths 1440,375` → `brokenImgs 0 / zeroBoxImgs 0 / thinImgs []`；供应链章正文抽查见 `.probe` 当次输出（歌尔/蓝思/龙旗/立讯/高通/奇景/博士眼镜，每段带 source_ids） |
| 2026-09-23 | W-114 | **体检要问"本篇现在能不能跑"，不是"引擎现在好不好"**：`bing_health.py --topic <领域>` 用本篇大纲自己的检索词做判据（`topic_controls()`：领域名 + 前两条点名领域的章节词），参照组只当参照；`--wait-healthy --topic` 同理，本篇自己的词点得到名就放行。为什么必须换判据：13:06 实测参照组 积家/咖啡/LV 三条全灭（整体 partial），同一分钟 娇兰 自己的词 33/35 点名，那一轮空章从 8 降到 2——等"所有品牌都好"是白等一个其实能干活的时间片 | 三条测试：`tests/test_audit_triage.py::test_topic_controls_ask_about_the_domain_being_generated`、`..._a_starved_brand_is_not_reported_as_a_bad_window`（`compare()` 只作诊断）、`..._the_domain_naming_its_own_subject_is_the_go_signal`。**这一条我自己推翻过一次**：第一版实现是"参照组活着而这领域零点名 = 品牌饿死 → 立刻放弃等待"，测试全绿之后 13:00 拿真引擎跑，报 `✗ 娇兰 品牌饿死，换时间没用`，而 13:06 同一批词就跑通了 6 章——判据把分钟级波动当成了永久属性，照它做流水会跳过一篇本来能做的稿子。改判据 + 改测试（`..._a_brand_with_no_answer_now_is_not_written_off_for_good` 钉住"零点名也不许提前收工"），实测表 `.probe/health_guerlain_2.txt` |
| 2026-09-23 | W-115 | **恢复工具也要过点名这道判据**：`recover_images.py` 默认不再把"从说明到来源页都没点名领域"的快照记录补回 data.json，要补得显式 `--force`。动因是我自己踩的：积家重做后只剩 1 张，我用这条命令补回 6 张，联络表一看是木工坊、永乐大钟、西铁城机芯、江诗丹顿机芯、佳明表盘——快照存的是**重做之前**那批图，而那批可能是主语闸门上线前的脏基线。回滚那边（W-109）早就拦了这件事，恢复这条通道漏了 | 测试先红在 `没点名领域的图被补回来了`：`tests/test_recover_images.py::test_recovery_refuses_to_restore_images_that_never_named_the_subject` + `..._force_is_the_only_way_to_backfill_an_unnamed_record`。夹具顺手修了一处不真实的地方：`_rec()` 造的 alt 里本来不带领域名，改成带（真实记录能进 data.json 就是先过了点名的），否则新判据会把七条旧测试一起判成假绿。`pytest tests/test_recover_images.py` 9 项绿、全量 exit=0；脏图已摘回：`.probe/strip_junk.py` |
| 2026-09-23 | W-113 | **明知会被否决的候选不再先下载**：`score()` 里所有 -999 判据只看标题与 URL（素材站/示意图/装修页/关键词命中不足/没点名领域），像素从不参与否决——尺寸与体积只参与加分。可 collect 一直是"先下载几 MB（原图失败还要再试缩略图）再打分"。实测代价：娇兰篇配图阶段跑了 **4.3 小时、2017 次丢弃、只拿到 2 张图**。拆出 `reject_reason()` 让 collect 在下载之前先问一句，判据一字未改。同一章（娇兰 shalimar，4 条检索词）改前改后：**5.3 秒、只下载 1 次、拿到 1 张** | 测试先红在 `明知会被否决的候选还是去下载了：[699pic…/ts/x…/other…/ts/x…/coffee…]`（3 条候选 5 次下载）：`tests/test_images.py::test_a_candidate_that_cannot_pass_is_never_downloaded`。重构中途踩了一次自己的坑：把否决段整体搬进新函数后，`score()` 尾还在用搬走的 `hit`，报 `NameError: hit is not defined`——拆函数要连调用方的依赖一起看，不能只搬代码。`pytest tests/test_images.py` 42 项绿、全量 exit=0 |
| 2026-09-23 | W-112 | **重做之前先等健康窗口，等不到就跳过**：`bing_health.py --wait-healthy --interval --max-wait`。动因是娇兰那轮——配图阶段烧掉 **1958 次丢弃、161 条"未取得合格配图"，只活下 2 张**。之前实测过更关键的一件事：**同一条检索词重复四次，拿回的是同一页垃圾**（`咖啡 手冲` 四次全 0 点名，而 `手冲 咖啡` 四次全 35 点名），所以"重试同一条词"在坏窗口里是纯浪费，能翻盘的只有换问法（W-110）或换时间（D-14）。现在把"换时间"做成机制，并且**到点必须返回失败**——不能让流水挂在一个永远不来的好窗口上。queue3/queue5 已改成先等窗口、等不到就跳过重做 | 两条测试用注入的 probe，不碰网络：`tests/test_audit_triage.py::test_wait_healthy_stops_polling_once_the_engine_is_usable`（第二次就健康，必须停止轮询）、`..._gives_up_instead_of_looping_forever`（累计 sleep 必须到 max_wait）；`pytest tests/test_audit_triage.py` 11 项绿 |
| 2026-09-23 | W-99 | **百达翡丽页面上的跑题图清干净了**（我 09-22 亲手回滚放进去的那批）。修法不是新规则，是把两处真错修掉：① W-109 逐章退回不再把"没点名领域"的旧图当"没变差"退回来——这正是柴油机、Calatrava 建筑、专利证书、万年历 APP、男孩肖像、法槌留在页面上的通道；② W-105b 引擎答非所问时不再解锁放宽主语那一档。重做之后 22 张全部点名领域、空章 0、闸门 0 fail / 0 warn。**验收没有只看闸门**：22 张拼成联络表逐张用眼睛看过（这正是当初骗过我一次的判据，这次两个都看了） | 前后对比 `.probe/sheet_patek.png`（23 张、6 张明显跑题）→ `.probe/sheet_patek2.png`（22 张、全部百达翡丽表款或自家工坊建筑）；`python scripts/audit_images.py 百达翡丽` = `✓ 配图 22 没点名 0 空章 0`（`.probe/patek_audit.txt`）；`python scripts/validate.py --domain 百达翡丽` = `0 fail / 0 warn`（`.probe/patek_val.txt`）。**残留**：同一来源页的近似图仍偏多（xbiao 系的 GIF 家族），W-111 的来源页配额是在这次重做之后才提交的，主链第二轮会带上 |
| 2026-09-23 | W-111 | **同一个来源页不许铺满整篇**：全站点 211 张配图里有 **17 个来源页跨章复用**，最坏的浪琴 5 个章节（history/master/movement/timing/value）全用同一个"购表作业"相册页，爱马仕 4 章、LV 3 章同源。同一页翻出来的照片机位、风格、水印都一样，读者连着看到就是"这图怎么又出现一次"。现在 `collect` 按来源页 URL 计数，一章一轮里用满 2 张就换页——**检查放在下载之前**，一条判据只看 URL，先问一句省掉整轮抓取。**没有做成闸门**：`aHash` 在汉明距离 ≤6 会把两张不同的黑白人像（劳斯莱斯 Charles Rolls vs 戴姆勒）也算成重复，模糊阈值不配当规则；而按 URL 数如果做成 G-03 fail，浪琴/爱马仕/LV 三篇会当场掉出索引（W-97 那种"改判据→大面积变陈"的机制），所以只在采集时生效，存量页面不动 | 测试先红在 `collect() got an unexpected keyword argument max_per_source_page`；**中途我自己写错过一次断言**（以为上限是 1 张，实测代码正确返回 2 张来自另一个来源页），改的是断言不是代码。`tests/test_images.py::test_one_source_page_does_not_become_the_pages_visual_identity`；夹具另修一处：`downloader` 原本对所有 URL 返回同一份字节，会被内容哈希认回成一张，测试就变成空转——现在按 URL 变字节。全量 exit=0 |
| 2026-09-23 | W-110 | **降粒度重试之前先试"换序"这一种问法**：09-23 实测（每组重复四次，全部稳定复现，不是抖动）`咖啡 手冲` 点名 [0,0,0,0] 而 `手冲 咖啡` 点名 [35,35,35,35]；反过来 `咖啡 烘焙` 好、`烘焙 咖啡` 坏。**方向没有规律**，所以不可能靠改写大纲一次做对——但换序是零成本的第二种问法，一次 HTTP GET。预算从 4× 提到 5×。**说清楚它救不了什么**：同一时刻对积家试了 4 组词的原序与换序（`积家 机芯`/`机芯 积家`/`积家 Reverso`/`Reverso 积家`/`积家 大师系列`/`大师系列 积家`/`积家 展厅`/`展厅 积家`），八次全部点名 0——积家今晚是引擎窗口问题，不是问法问题（23:49 同一批词里 `积家 专柜 等待 名单` 曾 34/34 点名，所以供给存在） | 测试先红在 `没试过换序形态：['咖啡 手冲', '咖啡', '手冲']`：`tests/test_images.py::test_fallback_tries_the_reversed_word_order`；改后 `pytest tests/test_images.py` 40 项绿、全量 exit=0；实测表 `.probe/order_probe.txt`、`.probe/reverse_live.txt` |
| 2026-09-23 | W-108 | **降粒度重试把英文虚词当检索词**：`Attention Is All You Need 论文` 逐词拆分后，`Is`/`All`/`You`/`Need` 各自变成一次真实检索。代价实测：大模型篇 training 一章 44 次检索里十几次是虚词，Bing 回《The Hate U Give》电影海报与语录站，**一章磨了 30 分钟零产出**，而 4×词数 的重试预算全烧在噪声上。加一张虚词表跳过；只收虚词，GPU/芯片/机芯/波导 这类领域名词必须留下 | 测试先红在 `sorted(bare) == ['All','Is','Need','You']`：`tests/test_images.py::test_granularity_fallback_never_turns_an_english_stopword_into_a_query`；改后 `pytest tests/test_images.py` 39 项绿、全量 299 项 exit=0 |
| 2026-09-23 | W-33b | **阅读时长口径定稿（决定，不是修代码）**：查出来的真问题不是"正文写长了"，而是**大纲里两个数从来不自洽**——每个大纲的 `reading_target_minutes` 写的都是 10，可 G-05 判的是 `words_range` 4500–6500 字，6500÷500=13 分钟，再加 20 张图×0.15 分 = 页眉必然 15–17 分钟。实测大模型篇 0 图 13 分钟、配满 22 图 16 分钟。按 D-11 开发者定调把需求 7 的"**最好**在 10m 左右读完"解释为软目标：不改正文、不改判据、不重生成；页眉继续显示真实计算值。**没有顺手把 words_range 对齐到 10 分钟**，因为那会让十篇存量产物当场被 G-05 判超长、索引大面积清空（W-97 现场），那是一次内容决策不是改常量 | 决定与算法推导见 `memo.md` D-18；数：`V.reading_minutes()` 对 10 篇产物实测 14–17 分钟（`.probe` 内当次输出），全部大纲 `reading_target_minutes=10` |
| 2026-09-22 | W-79 | **后台通知/监视器不能当证据**这条已经作为环境事实进 memo，无需代码动作：当晚两次收到"生成结束"事件，一次日志停在 44 字节、一次时间戳**晚于当时的系统时钟**（我核对时 19:01:39，它报 19:05:31）。同一批教训还多了一条：后台命令末尾接 `echo exit=$?` 会让**任务通知的退出码恒为 0**，09-22 大模型第一轮退 1 就是这么被瞒过去的 | 判活三依据与两条编码坑记在 `memo.md`「本地 CLI」与「测试日志」；本轮所有长跑改为看日志 size/mtime + 产物 mtime，并单独记退出码 |
| 2026-09-22 | W-105 | **空章要能说出为什么空**：`collect` 现在按大纲原词记 `{"offered": 召回几条, "named": 其中点名几条}`，落到 `output/<领域>/images/search-stats.json`（跨轮按词覆盖，不取历史最大——取最大会把后来一次引擎退化藏起来）；`audit_images` 读它，把空章分成 engine（答非所问）/ recall（零召回）/ supply（点名页被尺寸评分刷掉）/ unknown（没统计就说没统计）。报表行从「空章 4」变成「空章 4（引擎答非所问 2 / 供给 1 / 未知 1）」 | 三条测试各自钉住一个分支：`tests/test_audit_triage.py::test_an_empty_chapter_is_blamed_on_the_engine_when_it_answered_off_subject`、`..._with_naming_offers_is_a_supply_problem_not_the_engine`、`..._without_any_stats_is_reported_as_unknown_not_guessed`；再加一条报表测试 `test_the_report_prints_why_a_chapter_is_empty_not_just_that_it_is`（先红在实际值只有「空章 2」没有成因）。统计侧 `tests/test_images.py::test_collect_records_when_the_engine_answers_an_unrelated_query` + 反向的 `..._healthy_query_is_recorded_as_naming_the_subject` |
| 2026-09-22 | W-105b | **引擎答非所问时不再解锁「放宽主语要求」那一档**（这是百达翡丽跑题图的真正入口）：三档重试的第三档不带 subject，等于"只要像关键词就给过"。当这一章所有检索词的召回都是 `offered>0 且 named==0`（09-22 实测：`积家 机芯` 回的是汉语字典里「积」字的笔顺动画页）时，候选里**根本没有点名的图可挑**，放宽不是退一步而是保证跑题——那两张常柴柴油机、重型工作台就是这么配到百达翡丽页面上的。现在这种时刻这一章宁可空着（空章过不了 G-03、领域上不了索引，但脏图会直接上架骗读者），并在日志里写明"答非所问，换时间重试" | 测试先红，而且**第一版是假红**：夹具用 `b"ÿØ..."` 拼的假字节过不了 `accept_image`，两档都拿不到图，断言"没配进来"就白过了；换成 `test_images.jpg_bytes() + bytes(90000)` 的真 640×400 图之后，红在 `配进来 ['images/01_机芯_机芯.jpg']`——这才是要修的那个形状。`tests/test_integration.py::test_an_off_subject_answer_does_not_unlock_the_relaxed_tier` |
| 2026-09-22 | W-106 | **`scripts/bing_health.py`：跑图之前先量一次引擎**（判活不该靠猜，同 W-90）。5 条跨领域跨语言形态的控制词各一次 GET，报每条的 召回/点名，给 healthy/partial/dead 三态与退出码。写它的直接代价：09-22 晚上我在半死的引擎上跑了 50 分钟配图，347 条候选全死、只活下 8 张，而这条体检十几秒就能提前说破 | 实测（23:49）：`劳力士 手表` 35/35、`宾利 汽车` 12/12 健康，同一分钟 `积家 机芯` 34/0、`Louis Vuitton 手袋` 35/0、`咖啡 手冲` 1/0——**故障是按查询发生的，不是按品牌发生的**，所以"积家供给稀薄"这个结论从来没有被证明过。三态判定各一条测试：`tests/test_audit_triage.py::test_the_health_probe_calls_a_subject_naming_answer_healthy` / `..._all_off_subject_answer_dead` / `..._partial_without_calling_it_healthy` |
| 2026-09-22 | W-67 / W-70 / W-77 | **三条图片否决规则全部砍掉（不做）**：卖家词、设计词、车商列表页路径。这次没有拿单样本写规则——把全语料 184 张产物配图按三条规则各跑一遍，命中的 32 张拼成带标签联络表**逐张用眼睛看**。结果：卖家词命中 25 张里 0 张该否（LV 鞋/包/表、劳力士 Sky-Dweller、百达翡丽 Grandmaster、香奈儿 J12 全是正经产品图，只是页面恰好是价目页）；设计词命中 6 张里 2 张该否、误杀香奈儿作品展斜纹软呢与宾利特别版车照；车商路径命中 8 张里 0 张该否。**为 2 张真脏图要赔 27 张好图**。而且真脏的那两张（常柴柴油机、重型工作台）标题里根本没有这些词，它们能进产物只因为那一篇是 W-65 主语闸门上线前的旧产物，重做即被现有闸门拦下。W-77 的"外观侧身配内饰章"是章节语义错配，关键词/域名/路径在结构上都分不开 | 决策与量化表见 `memo.md` D-17；联络表 `.probe/sheet_flagged.png`（32 张带 [S]/[D]/[P] 标签与章节归属）；测量脚本口径：`output/*/data.json` 的 `images[].alt/caption`（collect 时由页面标题写入）+ `source_page` 路径 |
| 2026-09-22 | W-104 | **给快照记录补上消费者**：`scripts/recover_images.py <领域>` 从 `images.prev/records.json` 把硬杀打断的配图引用拼回去（先拷文件、后写引用，顺序反了等于亲手造"引用的图片不存在"）。两条判据宁可少补不补错：**只认 records.json**（manifest 没有 `section` 字段，凭它猜章节会把图发给错的章节，猜不出时还会安静产出"看起来恢复了"的 data.json）；**已有图的章节一律不动**（那些可能是这一轮新抓的更好的那批，正是 W-100/W-101 修的那类破坏）。默认 dry-run，`--apply` 才写。顺带把"data.json 里图片对象长什么样"收成一处 `generate.image_ref()`——schema 对图片是 `additionalProperties:false`、只认七个键，而 records 存的是 collect 整行（多带 source_image/title/bytes/format/section），照抄回去就是第二次事故 | 新文件 `tests/test_recover_images.py` 7 条：模块不存在时先整体红（collection error），实现后逐条钉住"引用回来且文件也在""query 不能丢（否则体检器不可分诊）""投影后合 G-10""不盖已有图""文件没了就不补且不牵连别的章节""默认不动盘""没凭据时退 2 且不写"。`pytest -q` exit=0（288 项） |
| 2026-09-22 | W-100 | **`collect` 会当场盖掉同名已存在的文件**（代码评审揪出的第一处，我在 W-85 只修了「先删再抓」，没修「盖写」这一处）。哈希没命中时它按序号取名落盘，序号只看本轮计数、不看目录里已经有什么；而 W-85 之后旧图是**故意留着不删**的，于是新图正好撞上 `45_咖啡_起源_埃塞俄比亚.jpg` 这种同名旧文件，`open(..., "wb")` 把已发布页面还在引用的那个字节整个换掉。修法：取名前把 `os.listdir` 的现有名字读进来，撞上就递增避让 | 测试先红：`tests/test_images.py::test_collect_never_overwrites_a_file_that_already_exists`（红在「collect 把已存在的 45_….jpg 盖掉了」，实测字节变了）；改后 `pytest tests/test_images.py` 全绿。教训：不变量是「任何时刻都不毁掉被引用的文件」，不是「不要先删再抓」——按后一种写法逐处补，一定会漏 |
| 2026-09-22 | W-101 | **配图清理的判据粒度错了**（评审第二处）：清理循环按「这一行检索没被采用」就删它指向的文件，可同一内容会被两行指向同一个文件（内容哈希认回必然出现），第一行在用、第二行没在用时，**正在用的那张被删掉**。改成两道闸：被任何一行采用的文件名一律不删；本轮没有真的写盘的文件（`fresh is not True`，即磁盘上早就存在的旧图）也不删。`fresh` 从整轮一个 bool 改成**逐行自带**（`collect` 写入时 `wrote = digest not in seen_hash`） | 测试先红：`tests/test_integration.py::test_a_used_image_is_never_deleted_because_another_row_pointed_at_it`；`fresh` 的契约单独钉在 `tests/test_images.py::test_an_adopted_existing_file_is_marked_not_fresh`。夹具替身同步补 `fresh`：它原先不吐这个键，等于替身没实现被替换函数的契约（本项目第 4 次踩同一条，已进 memo） |
| 2026-09-22 | W-102 | **索引把站点级问题算到每个领域头上**（评审第四处）：`_publishable` 用 `validate.run()` 的结果决定一篇能不能上入口页，而 G-11/G-12 这类 global 闸门只报一条整站 finding；原先按「有没有 fail」归因，站点级那一发会让**所有领域同时判不合格**、索引清空且日志一句解释都没有。改成只把 `where` 落在本领域目录里的 finding 记到该领域账上；「有候选却全被过滤」时 `build()` 直接抛错，不再安静产出一个空索引 | 两条测试先红：`tests/test_index.py::test_a_site_level_finding_does_not_get_blamed_on_every_domain`、`tests/test_index.py::test_build_refuses_to_silently_empty_the_index`；改后 `pytest tests/test_index.py` 17 项绿 |
| 2026-09-22 | W-103 | **旧图删除与快照留存的时机统一到发布之后**（评审 MAJOR-3，我上次只修了一半）：孤儿清理原先排在质量闸门**之前**，于是「闸门没过」这条最常见的失败路径恰好发生在图被删完之后；快照原先在 `finally` 里就 rmtree，等于闸门挂掉时连回到重做之前的凭据一起没了。真正的冲突不是顺序而是**判据**：只按当前 data.json 判孤儿，就必然要求赶在发布前删干净。所以 G-03 与 `_drop_orphan_images` 共用 `_page_image_refs`（磁盘上任何一页还在引用就不算孤儿），顺序才敢后移，快照才留到索引换新成功之后。代价写明：被旧日期页面引用的图会留着，这是「磁盘上每一页都打得开」的价格；同一天重做会原地覆盖文件名，所以不累积 | 两条测试先红：`tests/test_gates.py::test_G03_does_not_call_another_pages_image_an_orphan`（红在上一版页面在用的图被判孤儿）、`tests/test_integration.py::test_a_failed_gate_keeps_the_images_the_published_page_uses`（必须跨日期才暴露，同一天覆盖掉旧文件名就看不见；红在日志「清掉未被引用的旧图 12 张」紧接「闸门未过，未更新索引」）。改后 `pytest -q` exit=0（281 项）；`validate.py --all` 仍是 4 条真 fail（积家 4 空章）+ 大模型未产出页 |
| 2026-09-22 | W-95 | **体检器的分诊会把人支去改一份已经没毛病的大纲**：原来只有三档（检索词可救 / 引擎脏 / 无来路），而 `collect` 降粒度重试时会把检索词拆成裸词（`积家 机芯` → 裸词 `机芯`），用裸词拿回来的图必然没点名领域，却被算进「检索词可救」——**改大纲改不到它头上**，因为大纲根本没给过那条词。补第四档「点名档空手」，判据是**这条 query 是不是大纲给过的原词**（`offered[section_id]`），不是猜。| 新文件 `tests/test_audit_triage.py` 两条分支各自钉住，先红在 `KeyError: starved`；顺带把 `test_audit_triages_off_topic_images_by_query_provenance` 的夹具换成**真大纲原词** `磨豆机`（咖啡篇 grinders-machines 章，确实不点名）——旧夹具用的 `也门 摩卡港 历史` 根本不在大纲里（大纲写的是 `也门 摩卡港 咖啡 历史`，点名），所以那条断言一直在测一个不存在的形状。**改的是测试的前提，不是放宽断言。**真数据复测：积家 8 张跑题 → 「可救 0 / 点名档空手 8」，结论从「该改大纲」翻成「点名档确实空手，改大纲无用」，`.probe/tri_real.txt` |
| 2026-09-22 | W-84 | **批次中途重建索引会说出当下的假话**（LV 那次主页丢封面）——由 W-80 的第二种方案解决：索引收录每个领域之前先问闸门，闸门不过就跳过并打日志。于是「某领域正处于引用已清、新图未抓完的中途态」这种页面根本进不了索引，不需要给 `--offline` 加「只渲染不建索引」的旗标。09-22 实测：重渲染 6 篇的过程中，每次重建都只列当时真的合格的领域（一度 2 篇），最后一篇跑完回到 9 篇——中间态的索引是**诚实的小**，不是**虚假的大**| 见 W-80 的两条测试；过程日志 `.probe/refresh.log`；`validate.py --all` 15:40 实测 **10 个领域 4 fail / 3 warn**，4 条 fail 全是积家那 4 个空章，其余 9 篇 0 fail |
| 2026-09-22 | W-80 | **索引现在先问闸门再列页面**：`build_index._publishable(topic, root)` 调 `V.run(domains=[topic], skip=(G-13,), output_dir=root)`，有 fail 就跳过该领域并打日志。给 `validate.run` / `build_ctx` 补了 `skip` 与 `output_dir` 两个参数（G-13 要开真浏览器量七档视口，索引重建里等它不值；几何仍由 generate 的 verify 拦，判据仍只有 validate 一处）。动因是实测：积家 4 章没图、G-03 判死，索引照列它——因为索引只在「某领域自己 verify 通过」时重建，而门槛后来收紧了。| 两条新测试：`test_a_domain_that_fails_the_gates_is_left_off_the_index`（真闸门、不 stub，「全页没有来源」的领域必须被跳过）、`test_the_index_asks_the_validator_before_listing_a_domain`（钉**接线**：确实调了 run()、确实排了 G-13、确实只查这一个领域）；`pytest tests/test_index.py` 15 项绿；`python scripts/build_index.py` 实证跳过 8 个领域并逐条打出原因 |
| 2026-09-22 | W-97 | **改模板指纹会让全站产物一夜变陈**（W-80 修完当场暴露）：把 `bar_chart` 加进 BLOCK_TYPES 就变了模板指纹 → G-08 把 7 篇已完成产物全判死 → 新加的索引过滤把它们全踢掉，索引从 10 篇掉到 **2 篇**。不是过滤错了，是「改模板必须连带重渲染所有产物」这条纪律从来没进流程。已按 `--offline` 逐篇重渲染（6 篇，不花模型额度），`咖啡` / `浪琴` 顺带吃到 09-21 的新配色。| 日志 `.probe/refresh.log`；重渲染后索引应回到 9 篇（积家仍因 G-03 被正确排除）；**待办**：把「改模板 → 重渲染全部产物」写进 AGENTS.md 工作流，否则下次改区块还会再踩 |
| 2026-09-22 | W-96 | **重做时的章节互相抢图**（W-92 的根因，我上次只修了症状）：W-92 让退回阶段拒绝碰撞，于是「重复图失败」变成了「空章失败」——G-03 照样红。真机制在抓取阶段：`collect` 按内容哈希认回已落盘的文件，而 A 章先跑时 Bing 还给它的可能正是 B 章原来那张；等 B 章自己什么都没抓到、想退回旧图，那张已经在 A 章名下了。修法：`do_images` 收一份 `reserve`（各章原有图），**还没轮到的章节的旧图一律预留进 `claimed`**，前面的章节不许认走。顺带修一条自相矛盾的日志——它先打「不退」、下一行无条件打「已退回旧图」，看日志的人会以为退成功了，实际一张没退；现在报真实退回张数与被挡张数 | 测试先红：`test_a_redo_cannot_steal_a_later_sections_picture_before_its_turn`（替身如实复现：第一章认走第二章的旧图、其余章节这轮全空 → 第二章空章、闸门 1 fail）。**这条测试第一版是假绿**：我的替身没实现 `claimed` 语义，等于绕过了要修的机制——补上「被认领的文件不再给出」之后才真的红。改后 `pytest tests/test_integration.py` 27 项绿。实证动因：`redo3.log` 第 1170-1171 行 |
| 2026-09-22 | W-94 | **快照在两章共用一张图时自己把自己打死**：`_snapshot_images` 用 `_prev_files` 找旧文件，而它**先查 `images.prev/` 再查 `images/`**；两章引用同一张图时，第一条已经把它复制进快照位，第二条查到的就是刚放进去的那个路径，`copy2(src, dst)` 里 src==dst → `shutil.SameFileError`，整轮重做在起跑线上退 1（百达翡丽 09-22 上午那次根本没开始抓图就死了，我一开始以为是被 W-92 的重复图判死）。修法：绝对路径相同就跳过复制 | 测试先红：`test_snapshot_survives_two_sections_sharing_one_image` 一秒复现 `SameFileError`（比等一次 2.5 小时的真实重做快得多）；改后 26 项集成测试全绿；`redo3.log` 实证同一条路径现在打出「先备份旧配图 17 张」并进入抓取阶段 |
| 2026-09-22 | W-91 | 删掉 `coverOf()` 里那条**从来没生效过**的分支（`if (data.cover) return data.cover;`）与 `TopicData.cover` 类型字段。数据契约里没有 `cover`（`additionalProperties:false` 会拒）、十个真产物一个都没带、`build_index` 也不写它。留着不只是死代码：它会让抄夹具的人以为能手工指定封面，而一指定就被 G-10 判死。索引卡片自己的 `cover` 字段**保留**（那个是真在用的，由 `build_index` 写） | `cd web && npx tsc --noEmit` exit=0；`pytest tests/test_render.py tests/test_gates.py` 75 项绿；拿真产物重渲染确认封面仍然取到最宽章节图（`--cover-w:1920px` 的 img 还在 header.cover 里） |
| 2026-09-22 | W-92 | **逐章退回会造出「同一张图发给两章」**：W-72 的 `_rollback_images` 只问「这一章变少没有」，没问「这张图是不是已经被别的章节用走了」。重做时 B 章正好抓到 A 章的旧图，A 章没收获又整章退回 → 两章共用一张，G-03 当场判死。09-22 早上百达翡丽就红在 3 张 gif（印记 / 价格表 / 博物馆）。改成退回前先收齐「别的章节本轮已引用」的集合，冲突时**让给已经拿到图的那一章**（它这轮有收获，退回只该补缺口，不该抢图）并打日志 | 测试先红：`test_a_rolled_back_image_cannot_collide_with_another_section`（红在实际值 `['images/01_a.jpg','images/01_a.jpg']`）；改后绿，且断言 B 章的图没被抢回去；`pytest tests/test_render.py` 25 项绿 |
| 2026-09-22 | W-93 | **标题不许断词，长产品名在手机上顶破盒子**：积家篇 375px 下卡片标题 `Sphérotourbillon`（15 字符整词）需要 140px，而卡片内容盒只有 130px → G-13 报 `clipped-scroller`。模板给正文、表格、tag、图表文字都开了 `overflow-wrap:anywhere`，**唯独漏了标题**。修法是给 h1–h4 一起开——长商品名不是积家一家有（Grandmaster Chime、Reverso Tribute…）。诊断没靠猜：写 `.probe/why_h4.mjs` 直接把溢出元素的祖先链与计算宽度打出来才定位到 `.cards` 的 166px 轨道 | 浏览器实测同一个词造的 card_grid 页：修前 1 个 h4 溢出、修后 **0**；回归测试 `test_headings_are_allowed_to_break_a_long_word`（钉「标题在允许断词的选择器组里」这个一般性质，不钉某一页）；这条测试自己也先红过一次——**红在我的正则漏了 `{`**，不是 CSS 没修好，已改成按逗号拆选择器 |
| 2026-09-22 | W-68a | **LV 篇配图重做完成并过闸门**：12 张 / 4 章空 → **22 张 / 0 章空 / 0 跑题**，`validate --domain LV` 通过、索引重建为 9 篇。这是 W-85 非破坏式重做的第一次实战：全程 `data.json` 保留旧引用、旧图一张不删，中途那 2 张被哈希认回路径误删的图由 `images.prev/` 快照兜住 | `.probe/redo-queue.log` 复查行「✓ LV 配图 22 没点名 0 空章 0」；`output/LV/LV_2026-09-21.html`；`.probe/state.py` 复测 |
| 2026-09-22 | W-68b | **积家篇大纲检索词重写**（D-14 说它是「引擎问题、换时间重试」，这次数据证明还有一半天数是领域数据）：9 章里 27 条检索词有 19 条不点名领域，`positioning` 一章甚至直接搜「宝珀 真力时 对比」——**搜的是别家品牌**，加前缀救不回来。全部改成点名积家、≤4 词；`aliases` 从 `['Jaeger']` 扩成 `['Jaeger','LeCoultre','积家表']` | 改后实测：主语词 4 个、**不点名的检索词 0**、9 章每章都有 3 条点名档用得上的词；`pytest tests/test_contract.py` 全绿（含 `test_every_section_has_a_query_the_subject_gate_can_use`）；积家配图从 0 张涨到 7 张（第一轮重做，仍缺 4 章，正在第二轮） |
| 2026-09-22 | W-89 | 共享夹具 `tests/fixtures/mini_data.json` 自己违反数据契约，修好了。**我第一版诊断是错的**：jsonschema 报错时会把整个对象回显一遍，我看到回显里有 `heading` 就判它「字段不被允许」，还动手删了——`quote.heading` 契约本来就认。真原因是两条：顶层 `cover` 不在契约里（模板 `TopicPage.tsx:126` 读它，但十个真产物一个都没有，等于那条分支从来没被走过），以及 `quote.text` 只有 8 字而契约 `minLength=10`。渲染测试只出页面、不跑 schema，所以这漂移一直躺着。**危害不是测试不干净，是照抄样板写出的 data.json 会被闸门判死。** | 新增契约测试 `test_the_shared_fixture_satisfies_the_data_contract`（夹具必须 0 schema 错，以后再漂移送红）；夹具改动是**最小文本级编辑**：`git diff --stat` = 1 插入 / 9 删除（第一版我用 `json.dumps(indent=1)` 重排了整个文件、566 行 diff，已回滚重做）；`python -m pytest -q` exit=0 |
| 2026-09-22 | W-90 | `audit_images.redo()` 让重做变成黑箱：`capture_output=True` 加只回显最后 4 行，于是一次 20–50 分钟的重做**全程零输出**（09-21 实测日志 51 分钟停在 587 字节，判活只能摸 `images/` 的 mtime）；而且它按 utf-8 解码子进程输出，`generate.py` 重定向时写的是 cp936，那 4 行本身也是残缺的。改成不捕获、直接继承 stdout 流式输出，并用 `PYTHONIOENCODING=utf-8` 让子进程与父进程写同一句柄时编码一致 | `grep -n subprocess.run -A3 scripts/audit_images.py` 显示调用里已无 `capture_output`；`python scripts/audit_images.py 积家` 正常出报表；`pytest -q` exit=0。**注意**：正在跑的 LV/百达翡丽/积家 用的是改动前已加载的模块，下一次 `--fix` 才享受流式 |
| 2026-09-22 | W-85b | **我上一条 W-85 的"完成"说早了**：只堵住了 `_drop_images` 那一处删除，还有第二处。`collect` 按内容哈希会**认回**上一轮已落盘的同一张图（复用旧文件名，不重新写盘），而 `do_images` 结尾把"抓到却没派上用场"的图一律删掉——认回来的那张也在名单里，可它不是本轮产物，**已落盘的 data.json 可能还指着它**。修法：`collect` 给每条结果带 `fresh`（本轮是否真的写了盘），清理循环只删 `fresh is not False` 的。跑在旧代码上的那次 LV 重做当场留下现行证据：`04_路易威登.gif`、`08_Neverfull.jpg` 被这样删掉、页面 2 个破链（是 `images.prev/` 的快照兜住了恢复路径） | 两条测试先红后绿：`test_a_redo_never_deletes_a_file_the_published_data_still_uses`（红在"重做进行中删掉了已落盘引用还指着的文件"，日志里还能看见第一次删除成功、后面 11 次全报"孤儿图删除失败"——文件已经没了）、`test_an_adopted_existing_file_is_marked_not_fresh`（钉住 `collect` 的 `fresh` 契约：本轮写的 True、认回的 False；少了这条，集成测试里的假 collect 自己填 fresh 就测不到真代码）；`python -m pytest -q` exit=0（266 项） |
|---|---|---|---|
| 2026-09-21 | W-87 | **新增 `bar_chart` 区块（评分图）**：走的是"每个数字自己挂来源"那条路，不是抓榜单截图——截图进页面等于把数字交给读者自己核，违反红线 R-02。打通六处并互相用契约测试钉住：`$defs/barChart`（每根柱子 `source_ids` 为 required + `minItems:1`，至少两根，`as_of` 限 `YYYY-MM`）、两份 schema 枚举、`outline.BLOCK_TYPES`、`blocks.ts`、`renderers.tsx` 的 BarChart、提示词的区块结构表。G-06 加了自己的分支（不复用 `need_refs`：那条只说"含数字但无来源"加一截文本，对图表没人知道是哪根柱子）；G-13 把 `.chart` 拉进右边界比对组、把 `.chart .lab/.val/.axis` 拉进对比度清单（W-82 的教训：新增可见文字必须被量）。柱长按 `value/max` 算并**不截断坐标轴**，图下标注刻度与最大值 | 5 条新测试先红后绿：`test_a_bar_chart_draws_every_number_with_its_own_source`、`test_bar_widths_are_proportional_to_the_values`（断言的是比例关系不是像素）、`test_a_bar_without_a_source_is_rejected_by_the_schema`（自带防空转前提：完整图必须先能过 schema，另测空数组与单柱）、`test_G06_requires_every_bar_in_a_chart_to_be_sourced`、`test_the_chart_block_is_measured_for_right_edge`；`cd web && npx tsc --noEmit` exit 0；`python -m pytest -q` exit 0；截图人眼验收 `.probe/shots/chart_{wide,phone}.png`（手机档换成"名+分一行、柱子满宽"） |
| 2026-09-21 | W-86a | **大模型篇的大纲、主题与记忆点全部落地**（生成还没跑）。11 章：原理 / 怎么炼成 / 美国路线 / 中国路线 / 路线之争 / **Gemini 为什么强**（拆成多模态、架构规模、自有 TPU、数据配方与评测四层，并要求写明"哪层有公开证据、哪层只是推测"）/ **Jev 与 System One**（厂商宣称与独立验证强制分栏，"快 200 倍"必须标口径）/ **评分图怎么读**（bar_chart 落点）/ 价格与速度 / 怎么选 / 边界与风险。主题 `llm-attention`（品红 #B5177E + 孔雀绿 #2A9D8F）——不是拍脑袋：拿 G-11 自己的度量搜了 6 组候选，这组与既有 11 套的**最紧距离 65.8**（判死线 25），因为绿蓝金紫橙黑银红都已被占用。记忆点 `attention_matrix` 是 12×7 注意力方阵，亮格沿对角线 | `python -c "outline.load_for_topic('大模型')"` → 11 章、大纲 schema 0 错；主语词 8 个全部由 `aliases` 收进来（`subject_tokens` 实测）；`sweep_tokens` 现在 **24/24 过 G-13**（含 `llm-attention` 与 `-motif` 两态）；`test_the_real_theme_set_is_not_a_wall_of_twins` 覆盖 12 套主题仍绿；截图 `.probe/shots/cover_wide.png` |
| 2026-09-21 | W-86b | 记忆点第一版是**错的**：`(r*5)%COLS` 的步长把亮格打成锯齿，看着像随机撒的，不像"注意力集中在对角线"。改成 `round(r·(COLS-1)/(ROWS-1))` 的真对角线。跟娇兰那次"两个黄点"是同一类问题——**只有截图看得见**，G-13 两态全绿也照样放过 | 改前/改后截图对比 `.probe/shots/cover_wide.png`；改后 `npx tsc --noEmit` exit 0、`pytest -q` exit 0 |
|---|---|---|---|
| 2026-09-21 | W-88 | **G-11 是一条永远全灭的闸门**：判据把 `bg` 也平均进 ΔE（两套主题共用纸白底是应该的，实测两片米白底只差 1.7），阈值 60 从没对着真色板量过——`--all` 报 42/55 对"雷同"，把 52 条真 G-03 fail 埋在里面，而 R-06 规定 `--all` 不全绿不许标任何领域完成。改成只看**身份色**（primary + accent）且**两个都近**才算雷同（均值会让一个极端色替另一个蒙混：劳斯莱斯 vs 积家 primary 61.5 / accent 12.3 就是"两种几乎一样的金"）；阈值 25 按 11 套真色板分布定（ΔE≈25 = 同色系换色调）。顺带堵一个静默漏洞：`palette_of` 以前把取不到的角色滤掉，字段数不同的两对直接 `continue` 跳过——那是"没测"混进"测过且通过"，现在按角色返回、取不到身份色就单独报。| 两条新测试先红：`test_G11_ignores_a_shared_background_when_identity_colors_differ`（夹具就是实测假阳性 劳力士 vs 浪琴：底色 1.7、主色 46，旧量法判"雷同 19.3<60"）、`test_the_real_theme_set_is_not_a_wall_of_twins`（红在 42 对）；`python scripts/validate.py --all` G-11 **34 → 0**，全站 fail **93 → 59**；`python -m pytest -q` exit=0（259 项）；红线 R-09 侧：`sweep_tokens` 11 主题 × 有/无封面共 **22/22 过 G-13**，另截图人眼看过去重色 |
| 2026-09-21 | W-88b | 修完判据后露出**三对真·雷同主题**（不是阈值问题，是四套奢侈品主题在用同一张皮：奶油底 + 深中性主色 + 金强调色）：LV↔咖啡（12.6/18.4）、LV↔香奈儿（17.5/15.9）、浪琴↔积家（19.9/21.6）。按"改色板而不是改阈值"处理，且每一处都往品牌本来的样子靠：LV 主色 `#33261A`→`#6E5A38`（Monogram 涂层帆布是橄榄褐不是近黑，顺手把 `label` 从"焦糖花纹与炭黑"改对）、咖啡强调色 `#C98A28`→`#1E6B52`（生豆/咖啡叶的绿，把金色位让给真正拥有它的四个品牌）、浪琴强调色 `#8B97A3`→`#C8102E`（带翼沙漏的浪琴红，label 同步改），积家/香奈儿的深蓝与黑白原样不动 | 三对全部拉开：改后最紧的一对是 宾利 vs 浪琴 max=34.4（判死线 25）；`node scripts/shot.mjs` 1440 档实测三页 `brokenImgs:0 / zeroBoxImgs:0 / thinImgs:[]`，截图人眼确认（浪琴封面那张 GMT 恰好有橙色指针，新红色对上了）；`.probe/shots/crop_top_{咖啡,LV,浪琴}.png` |
| 2026-09-21 | W-85 | `--redo-images` 从"先删再抓"改成非破坏式：`_drop_images`（删文件）换成 `_clear_image_refs`（只清内存引用），**旧图一张不删**；清空后不再立刻落盘（磁盘上那份 data.json 一直指着一直存在的文件）；孤儿清理 `_drop_orphan_images` 从配图阶段挪到**新页面渲染成功之后**。这样"任何时刻被硬杀"都成立，而不是只有"跑完之后"成立。快照记录落盘成 `images.prev/records.json`（带 section 与 query）。**台账里那条"write_manifest 不写 query"经实测是错的**：`咖啡/images/manifest.json` 有 query（8 键），百达翡丽/劳力士没有是因为它们的 data.json 记录产自 W-71 之前——字段本来就会随记录写出去，不需要改代码 | 三条新测试全部先红后绿：`test_a_hard_kill_in_the_middle_of_the_redo_leaves_the_page_whole`（红在"重做进行中落盘的 data.json 已经丢了旧引用"＋"旧图已被删除"＋"images.prev/ 里没有 records.json"）、`test_the_snapshot_records_keep_the_query_that_found_them`、`test_a_failed_render_keeps_the_images_the_published_page_uses`（红在渲染失败后 `01_stub.jpg…` 全没了）；`python -m pytest -q` exit=0。**动因是实测事故**：09-21 检查 output/ 时百达翡丽 data.json 引用 0 张、images/ 里 19 张、images.prev/ 里 17 张、已发布页面 18 个破链，最后一次写盘 02:27 |
|---|---|---|---|
| 2026-09-20 | W-82 | 两件"看起来坏"和"悄悄不测"一起修：① 索引里**无封面的卡片**以前只有一块 `aria-hidden` 的品牌色渐变，读者只能理解成"图裂了"（开发者在主页上就是这么报的）——现在改成排版封面：领域名大字压在渐变浅色带上，并**把 `.ph-word` 加进 G-13 的对比度清单**（新增可见文字必须被量，不然就是下一个漏网的）；② `sweep_tokens.py` 的主题清单是**写死的 10 个**，加第 11 个领域不会报错、只会静默不跑，"每套主题都过 G-13"于是变成假话——改成从 `web/src/tokens/index.ts` 解析 | `test_a_card_without_a_cover_is_typographic_not_an_empty_frame`（先红：无封面卡里没有排版层）+ `test_every_outline_token_exists_in_the_typescript_themes`（带自检：解析不出主题时不许一路绿灯）；`node scripts/measure-layout.mjs --file output/index.html` → 布局几何检查通过；`sweep_tokens` 现在解析出 11 套主题（含 `guerlain-abeille`）并跑 22 个产物 |
| 2026-09-20 | W-81 | 一条列只剩一个右边界（开发者指出"字、图、表格向右不一样，看着不平整"）。三处一起改：① 内容列去掉预留的 `+24rem`（那 384px 就是表格比正文宽出来的量），② `--measure` 44→46rem 且 `.figs/.steps/.timeline` 不再单独 50rem、与表格卡片同宽，③ 外壳从 84.5rem 收到 62.5rem，封面右边缘与正文右边缘一起收口。**并且把闸门修到能守住它**：`G-13/measure-inconsistent` 以前只量 `.body > *`，而区块都包在 `.blk` 里 → 量到的全同宽、参差在下一层；现在把 `.tablewrap/.figs/.note/.cards/.facts/.steps/.timeline` 全拉进同一组比 | 拿 **git 里的旧版页面**跑新规则，它当场变红并给出开发者说的那件事的数字：`@1440 右差 356.8px`（`.body>*`/`.tablewrap`/`.cards`/`.facts`=1061、`.figs`/`.steps`/`.timeline`=800、`.note`=704）、`@1920/@2560 右差 384.0px`——**规则改完先证明它能抓旧版，再说新版通过**；新版 `劳斯莱斯`/`咖啡` 走 `--offline` 重渲染后 `0 fail / 1 warn`（warn 是已知的 W-33b 偏长），几何基线重录 375/1440/3762 三档，全量 `pytest -q` 绿 |
| 2026-09-20 | W-33 | 文章页第六版**开发者已确认**（正文锁 44rem、目录进左栏、配图封顶 800px、小图不放大成糊图）：`python scripts/open.py` 打开 `output/咖啡/咖啡_2026-09-20.html` 与 `output/index.html` 后回"主页还行"、"咖啡的也不错"。几何侧的对照数据：375/1440/2171/3762 四档 0 破图 0 零盒 0 窄图，3762 档正文列宽锁在 1352px | 开发者原话（本轮对话）；`.probe/shots/咖啡_2026-09-20_{375,1440,2171,3762}.png`；`node scripts/shot.mjs` 输出的 `brokenImgs:0 / thinImgs:[] / wrap.w` |
| 2026-09-20 | W-78 | 劳斯莱斯最后一章 `culture` 两轮失败的真因分开定位：qodercli 是 `timed out after 1500 seconds`，copilot 是三次只回横幅 `Color output disabled.`（正文 0 字节）。**pass2 自己把这一章救回来了**（10/10 章、culture 正文 2180 字符），所以不需要拆章也不需要动提示词。真正修掉的是诊断盲区：`llm.py:_run` 以前只在「退出码非 0 且 stdout 为空」时报错，于是上层报"响应里找不到合法 JSON：Color output disabled."，**stderr 里的真因整个丢掉**，看着像模型的锅其实是后端的锅 | `test_run_surfaces_stderr_when_stdout_has_no_payload` + `test_run_still_raises_on_nonzero_exit_even_with_junk_stdout`（两条都先红在 `DID NOT RAISE`）；commit `6de121e`；探针实测 copilot 单发能回 51KB 正常 JSON 事件流，所以不是兜底后端坏了；`output/劳斯莱斯/data.json` 10 章 |
| 2026-09-20 | W-76 | 点名档的检索词构造收到一处（`images.gated_queries`）：一条词要么本身点名领域，要么短到能被 `ensure_subject` 补上主语，两条都不满足的**补一条「品牌 + 前两个词」的短兄弟**（不替换原词，原词在放宽档还有用）。这是 W-75 那个"百达翡丽 7 章配回垃圾"的系统版——不是那 7 章写坏了，是全语料都有这种词 | 实测 `315 条里 125 条（40%）永远进不了点名档`，补兄弟之后**十个领域每一章都至少有一条点名档用得上的词**（仍无可用词的章节：无）；`test_long_queries_get_a_short_sibling_that_names_the_subject`（先红：`AttributeError: module 'images' has no attribute 'gated_queries'`）+ 契约测试 `test_every_section_has_a_query_the_subject_gate_can_use`；全量 `pytest -q` = 249 passed |
| 2026-09-20 | W-73 | 集成测试不再改写入库产物 `output/index.html`：`build_index.build(date, output_dir)` 由 `generate.py` 传自己的 `OUTPUT`（唯一归属），命令行 `python scripts/build_index.py` 行为不变。顺带定一条操作纪律：**别在 pre-commit 跑套件的同时改源码**——上一次提交跑到一半时我正在编辑 `generate.py`，它绿了是运气不是证据 | `test_a_generated_domain_does_not_rewrite_the_real_index`（先红：红在第二条断言"索引没建到临时目录"；只比内容比不出问题，因为重建出来的字节恰好一样——这条测试如果只断言内容就是空断言）；全量 `pytest -q` 之后 `git status` 里 `output/index.html` 不再出现 |
| 2026-09-20 | W-75 | 跑题图的来源查清并分三层修掉：① 体检器把"没有来路"当"检索词可救"（`query` 缺失时 `any(...)` 恒假）→ 分诊加 `unknown` 一类；② 134 张已采用图实测：没点名的 13 张关键词命中**全部 ≤1**，点名的 121 张有 37 张命中 1 → "至少两个词"只加在**放弃主语那一档**（`images.RELAXED_MIN_HITS`），不再重演加在点名档误杀真图那次翻车；③ 根因在领域数据：百达翡丽 7 章 + 浪琴 1 章的 `image_queries` 既不含品牌词又每条 ≥3 词（`ensure_subject` 只在补完 ≤3 词时才补），点名档必然空手、放宽档必然收垃圾 → 改 `config/topics/*.json` 不改代码 | `test_an_image_without_provenance_is_not_counted_as_fixable`（先红：`[('images/b.jpg', None)]` 进了 fixable）+ `test_the_subject_free_tier_needs_two_keyword_hits` + `test_the_relaxed_tier_carries_the_keyword_floor`；分布表见 `memo.md`「13 张跑题图到底从哪一档进来」；`git diff config/topics/` 8 章检索词 |
| 2026-09-20 | W-72 | 重做配图不再能把页面清空：`--redo-images` / `--only` 清空引用之前先备份到 `images.prev/`，配图阶段结束（含抛异常）后**逐章比较，哪一章变少了就把那一章退回旧图**。判据只有"变少了没有"，不设"召回要够 N 条"的健康线——实测同一句检索词两次分别返回 5 条和 35 条，任何单点阈值都是在给噪声定指标（D-14 同理） | `test_a_crashing_redo_cannot_take_the_page_down_with_it`（先红：`! 全站仅 1 张配图，低于下限 8（G-03 会判死）`）+ `test_a_useful_redo_keeps_the_new_images_and_loses_the_backup`（有收获时必须用新图、删备份）；`scripts/generate.py:333` 起；`memo.md` 15:50 节。顺带修好一条**永远不会红的测试**：`test_redo_images_persists_the_clear_before_recollecting` 把路径拼成 `images/images/01_stub.jpg`，而它盯的"清空后先落盘"又让 refs 恒为空，循环一次都没进过 |
| 2026-09-20 | W-71 | 每张配图记住自己的来路：`data.json` 的 image 记录与 `images/manifest.json` 各带 `query` 字段（schema 的 images 项放开这一个键）。体检器现在能分诊"检索词没点名领域（改大纲有救）"vs"点名了还给脏图（引擎问题）"，不用再靠"哪个领域是哪段代码跑的"推理 | `test_section_images_keep_the_query_that_found_them` + `audit_images.py` 的 fixable/dirty 分诊（今天实测：百达翡丽 12 张跑题全部判"检索词可救"）。**已知边界**：字段是这一轮才加的，只有之后重做过的领域带（宾利 20 张里 8 张有，其余是加字段前的 pass1 产物，`--fix` 重做后补齐） |
| 2026-09-20 | W-69 | 品牌别名改由领域数据声明：`config/topics/<领域>.json` 新增可选 `aliases`，主语集合按「领域名 → 声明的别名 → 推导词」合并。推导只收"在 ≥3 章检索词里露过面的拉丁词"，所以 rolex/hermes（各 2 章）收不到，而降档会把咖啡篇的"巴拿马"当主语——文档频率本来就不是"专有名词"的判据。顺带堵住 LV：领域名 "LV" 两字母当子串会命中 involve/resolve，纯 ASCII 短于 4 一律不收，主语集合现在 LV=['vuitton'] | `test_declared_aliases_join_the_subject_set` + `test_short_latin_topic_name_does_not_become_a_subject`（先红：`['lv','vuitton']`）+ `test_outline_schema_permits_aliases` |
| 2026-09-20 | W-65b | 咖啡篇按新规则重做配图后复验：没点名主语的图从 6/12 降到 1/12（剩下那张是放宽档救场的），`validate.py --domain 咖啡` 回到 0 fail 0 warn，shot 实测 13 图 0 破图 0 窄图。中途 grinders-machines 一章两档都没图导致 G-03 判死，量了才知原因是素材站占 34/35 候选 + Bing 对多词中文查询只召回 1 条，**不是** `min_width` 太严（被否候选里最宽的只有 476px） | `output/咖啡/咖啡_2026-09-20.html`；`.probe/shots/咖啡_2026-09-20_1440.png`；`test_granularity_fallback_also_tries_the_bare_token`；commit `1f37141` |
| 2026-09-20 | W-65 | 配图相关性判据从"命中几个词"改成"有没有点名领域本身"：两篇闸门全绿的页面里 34 张图有 15 张跑题（中国行政区划、也门铁修剪、巴拿马展园、50张肖像摄影）。主语词从大纲检索词推导（≥3 章出现的拉丁词），不写品牌别名表；一章实在没有就退回不带主语的一轮，G-03 要求每章有图 | `test_off_topic_images_rejected_even_without_naming_the_subject` + `test_naming_the_subject_is_enough_to_keep_an_image` + `test_subject_tokens_are_derived_from_the_outline_not_hardcoded`；回放表 咖啡 6/6、香奈儿 13/9 零误杀（`memo.md` 配图节，含被否掉的"≥2 词"第一版 3/12、1/22）；commit `0d4cc5a` |
| 2026-09-20 | W-66 | `--redo-images`：只重做第 3 阶段（正文不重跑、不花模型额度）。不清空旧图就是假旗标——`do_images` 会跳过"已经够图"的章节；顺带修素材站黑名单按 TLD 写死（`nipic.com` 在列，同一张图从 `nipic.cn` 漏过） | `test_redo_images_recollects_sections_that_already_have_images`（断言新旧文件集合不相交）+ `test_stock_site_veto_covers_every_tld_of_that_site`；commit `a89c3ea` |
| 2026-09-20 | W-64b | 香奈儿篇真产物出炉：11 章 / 22 张配图 / 0 fail 0 warn（修 schema 前它写完 11 章仍出不了页） | `output/香奈儿/香奈儿_2026-09-20.html`；`.probe/shots/香奈儿_2026-09-20_*.png`（0 破图 0 放大） |
| 2026-09-20 | W-64 | `data.json` 改原子写（`.tmp` + `os.replace`）：中断不再把已花额度的十几章内容变成半截 JSON；顺带定死"停批量任务要杀脚本树不是 python 树" | `test_save_keeps_the_last_good_data_when_a_write_fails`（先红：坏数据落盘成 `'{\n "bad": '`）；`memo.md` 09-20 凌晨节 |
| 2026-09-20 | W-37b | 视觉基线改钉在**真产物**上：从 `output/咖啡/data.json` 渲染后量，不再量 demo 桩件正文（桩件页 1440 档整页 6718px，真页面 30798px——旧基线根本量不到真版式）；基线缺失才 skip，产物缺失直接失败，不留假绿 | `tests/golden/layout-coffee.json`（13 图 / 0 破图）；`_current()` 的 DATA 断言 |
| 2026-09-20 | W-18 | 咖啡端到端真跑通：12 章 / 6441 正文汉字 / 12 张配图 / 83 条来源，`validate.py --domain 咖啡` = 0 fail 0 warn；G-13 在 7 档视口全过（含强制载入懒加载图后） | `output/咖啡/咖啡_2026-09-20.html`；`.probe/shots/咖啡_2026-09-20_*.png`；`memo.md` 咖啡出炉节 |
| 2026-09-20 | W-62 | "约 N 分钟读完"两把尺子合一：`validate.reading_minutes()` 唯一归属，生成时写进 `data.reading_minutes`，模板与索引只显示 | `test_page_shows_the_same_minutes_the_index_computes` + `test_reading_minutes_field_wins_over_the_templates_own_count` |
| 2026-09-20 | W-63 | 度量工具假绿修复：G-13/shot 量之前先滚动强制加载 lazy 图，"图片放大"不再静默跳过折叠以下内容 | `scripts/measure-layout.mjs` forceLoadImages |
| 2026-09-19 | W-61 | 生成档位参数化：`--minutes` / `--sources-per-section` / `--fast`，**默认仍是高质量**；`--minutes` 走单个环境变量同时喂给提示词与 G-05，顺带修掉"每章预算读 config、闸门读大纲"这个只在 10 分钟时恰好一致的分叉 | `tests/test_integration.py`（2 项）+ `test_section_budget_follows_the_outline_not_just_the_config` + `test_reading_minutes_env_override_moves_both_sides`；`AGENTS.md` 档位节 |
| 2026-09-19 | W-37 | 视觉基线：比的是**实测几何指纹**不是像素（像素 diff 会被字体/抗锯齿噪音淹掉）；`shot.mjs --json-out` 出基线，比对失败即报"哪个视口、哪个盒、从多少变到多少" | `tests/golden/layout-coffee.json`；`tests/test_visual_baseline.py`（2 项，含"基线自己能不能被证伪"） |
| 2026-09-19 | W-42b | 研究中间文件清理策略定了：可追溯性靠 `source_page`/`source_image` URL，不靠网页副本；`scripts/prune_scratch.py` 默认 dry-run，删必须显式 `--apply` | `tests/test_prune_scratch.py`（3 项）；实测 63.9 MB / 0 个过期 |
| 2026-09-19 | W-11b | `--all` 现在也跑索引页的 G-13（以前只跑 G-12，索引跑版没人知道） | `validate.run()` whole_site 分支；`test_all_mode_runs_geometry_gate_on_the_index`.replace(" ","") |
| 2026-09-19 | W-28 | 新领域零代码验证通过：只写 `config/topics/<领域>.json`（不改任何 .py/.ts）即可出页面并过 G-13 | `tests/test_new_domain.py`（2 项，造了一个清单外的"抹茶"领域） |
| 2026-09-19 | W-50 | 10 套主题 × 有封面/无封面共 20 个产物全过 G-13；实测翻车的不是深色而是浅色——7/10 套 kicker/编号/tag 只有 2.0–3.8:1 | `scripts/sweep_tokens.py`；`tests/test_tokens_contrast.py`（2 项，不开浏览器）；commit `d20958e` |
| 2026-09-19 | W-60 | G-13 新增 WCAG AA 对比度检查，墨色改为按对比度算出（`--accent-ink`/`--primary-ink`/`--muted-ink`），装饰色保留原强调色 | `styles.ts` inkFor；`test_G13_detects_low_contrast` 注错自证 |
| 2026-09-19 | W-52 | 产物 `<style>` 移进 `<head>`（SSR 原先把它渲染进 body：不合规且静默覆盖后插样式） | `web/src/ssr-entry.tsx` + `hideStyle`；`test_style_lives_in_head_not_body`；commit `7e8d3e6` |
| 2026-09-19 | W-53 | 无封面时领域记忆点铺满整块封面（roast 五段带 / 星点 / 瓷砖 / 斜向拉丝），不再是"一条色带浮在空色块里" | `styles.ts` `.cover-noimg *`；20 例扫描含 10 个 `-motif` 态 |
| 2026-09-19 | W-59 | 看页面统一走**系统默认浏览器**（Edge），不写死可执行文件；量几何的内核改为优先 Edge（`browser.mjs` 探测），G-13 现在在开发者实际用的内核上跑 | `scripts/open.py`、`scripts/browser.mjs`；`tests/test_browser.py`（3 项，含"源码里不许出现 .exe"） |
| 2026-09-19 | W-43 | 成本口径定案：接受后台跑数小时 + 全程联网查证，不砍章节、不限每章来源数（后续由 W-61 参数化让开发者自己调档） | 开发者原话「①接受后台跑数小时」「默认还是高质量吧」；`memo.md` D-11/D-12；`.probe/gen-all.sh` |
| 2026-09-19 | W-44 | 9 条候选红线全部生效，每条挂检测器；G-09 改为"红线是否还挂着有效检测器" | `rule.md` 第一节 R-01…R-09；`tests/test_redlines.py`（12 项）；`test_R_active_lines_are_parsed_by_validate` |
| 2026-09-19 | W-45 | `git init` + 版本化 pre-commit 钩子（只跑 pytest） | 首个提交 `f1e41fa`；`scripts/git-hooks/pre-commit`；`.gitignore` |
| 2026-09-19 | W-42 | 联网研究落盘隔离：`llm._run()` 固定 `cwd=.research-scratch/`（此前只是手动挪脏文件，代码没隔离） | `scripts/llm.py` SCRATCH；`tests/test_llm.py::test_cli_runs_in_scratch_dir_not_project_root` |
| 2026-09-19 | W-51 | G-12 锚点正则里有一个不可见退格符 `\x08`（`\b` 被转义），导致锚点检查永不命中；改 `\s+` 并把失败信息改成 `str(f)` 才看得见 | `scripts/validate.py:546`；`test_G12_passes_on_a_good_index` 现绿；`memo.md` 第六轮 |
| 2026-09-19 | W-33a | 文章页自适应根因修复：宽度给结构（目录栏 + 图）而不是给句子；正文 44rem、单图 800px、显示宽 ≤ 原图 1.35× | `web/src/tokens/layout.ts`、`styles.ts`；G-13 七档全绿；`memo.md` 第六轮改前/改后表 |
| 2026-09-19 | W-54 | G-13 新增三条宽屏几何检查并各自注错自证 | `measure-layout.mjs` prose-measure / img-upscale / fig-wall；`test_G13_detects_wide_screen_defects`（4 例） |
| 2026-09-19 | W-55 | 自查工具 `scripts/shot.mjs`：五档视口截图 + 实测几何（含"原图被放大到多少 px"），不再依赖开发者截图报 bug | `node scripts/shot.mjs <html>`；`.probe/shots/*.png` |
| 2026-09-19 | W-49 | 封面与记忆点关系收口：`coverOf()` 只接受 ≥1150px 的原图当封面，不够大就退到领域记忆点（索引页同规则，两处不会换图） | `TopicPage.tsx` COVER_MIN_WIDTH；`build_index.cover_of`；`test_image_too_small_to_be_a_cover_is_rejected` |
| 2026-09-19 | W-56 | 配图取源改为尺寸优先（≥1600 +6 / ≥1150 +4），并硬拒 logo/壁纸/广告大片/海报（红线 R-04） | `images.py` score()/DECOR_HINTS；`test_R04_rejects_logo_wallpaper_and_ad_creatives` |
| 2026-09-19 | W-57 | 镜像源写进仓库而不是只配在机器上：`web/.npmrc` + `requirements.txt --index-url`（红线 R-07） | `tests/test_redlines.py::test_R07_*` |
| 2026-09-19 | W-58 | 无来源图片不再可能进产物：`source_page` 进 schema required（红线 R-03） | `config/topic_data.schema.json`；`test_R03_data_schema_rejects_image_without_source_page` |
| 2026-09-19 | W-01 | 治理四件套建立 | `AGENTS.md`/`rule.md`/`memo.md`/`HANDOVER.md`；`tests/test_smoke.py` |
| 2026-09-19 | W-02 | 镜像源固化：npm→npmmirror、pip→清华+阿里 | `npm config get registry`；`pip config list`；`memo.md` 环境事实 |
| 2026-09-19 | W-08 | 模板快照 + 跨主题配色不雷同断言 | `validate.py::G-11`；`test_theme_distance_flags_twin_palettes` |
| 2026-09-19 | W-29 | 索引主页：单一连续 feed + 类别 chip + 卡片下钻 | `web/src/template/IndexPage.tsx`、`scripts/build_index.py`；G-13 七档通过 |
| 2026-09-19 | W-30/W-31 | category + tags 进两份契约，10 份大纲全配齐 | `config/topic_outline.schema.json` required；`test_every_seed_outline_declares_category_and_tags` |
| 2026-09-19 | W-32 | 索引闸门 G-12（链接可达、封面存在、类别可见且跳得到） | `validate.py::g12_index`；`test_G12_*` 4 项反例 |
| 2026-09-19 | W-34 | 排版字重改造：display 500 + 正字距，小字加重，表头去深色块 | `web/src/template/styles.ts`；`test_layout_grows_with_viewport_instead_of_pinning` |
| 2026-09-19 | W-35 | 章节改为图先于文，封面比例轮换 | `TopicPage.tsx`/`styles.ts`；`test_figure_box_spans_the_column_and_keeps_landscape_shape` |
| 2026-09-19 | W-36 | tag pill 与笔记式 meta 行进入专题页 | `TopicPage.tsx`；`test_title_is_present_and_not_trapped_in_cover_box` |
| 2026-09-19 | W-46 | code review 查出 4 BLOCKER+4 MAJOR 全部修复 | 见 `memo.md` 第五轮表；`tests/test_render.py` 5 项新断言 |
| 2026-09-19 | W-47 | 新增 G-13 真浏览器几何闸门，反向自证 5/5 注错被抓 | `scripts/measure-layout.mjs`；`test_G13_detects_a_deliberately_broken_page` |
| 2026-09-19 | W-48 | 修 review 途中自造的嵌套 minmax 塌列，并加固 G-13 | `test_content_grid_tracks_can_shrink_and_wrap` |
| 2026-09-19 | W-03 | pytest 骨架 + 治理文档存在性冒烟测试 | `python -m pytest -q` → 112 passed；`tests/test_smoke.py` |
| 2026-09-19 | W-09 | 数据契约：大纲 schema + 数据 schema + 10 类区块封闭枚举 | `config/topic_outline.schema.json`、`config/topic_data.schema.json`、`tests/test_contract.py`（7 项） |
| 2026-09-19 | W-04 | `web/` Vite + React + TS 工程（npmmirror 源） | `cd web && npx tsc --noEmit` → 0 错误 |
| 2026-09-19 | W-05 | 10 套主题 token（每领域一套配色/字形/motif） | `web/src/tokens/index.ts`；`test_tokens_cover_every_topic`、`test_every_topic_has_its_own_hero_motif` |
| 2026-09-19 | W-06 | 模板组件树：TopicPage + 10 类区块渲染器，未知类型抛错 | `web/src/template/{TopicPage.tsx,renderers.tsx,styles.ts}`；`tests/test_render.py`（9 项，含 `test_unknown_block_type_fails_loudly`） |
| 2026-09-19 | W-07 | 工作台预览页（喂 data.json、切 10 套主题） | `web/src/workbench/main.tsx`、`web/index.html`、`web/vite.config.ts` |
| 2026-09-19 | W-13 | SSR 静态化：esbuild 转译 + renderToStaticMarkup + CSS 内联 | `scripts/render.mjs`；`test_render_is_self_contained`、`test_render_has_no_runtime_js` |
| 2026-09-19 | W-12 | 图片管线：Bing 解析/字节校验/尺寸读取/murl→turl 兜底/去重/素材站硬拒/manifest | `scripts/images.py`；`tests/test_images.py`（16 项，用录制的真实 Bing 页面做 fixture） |
| 2026-09-19 | W-10 | LLM 适配层：qodercli→copilot 兜底、prompt 走 stdin、JSON 三级容错、联网判据 | `scripts/llm.py`；`tests/test_llm.py`（16 项，零真实调用） |
| 2026-09-19 | W-14 | 质量闸门 G-01…G-11，`--all`/`--gate`/`--list`，唯一裁判 | `scripts/validate.py`；`tests/test_gates.py`（35 项正反例）+ `tests/test_gate_matrix.py`（9 项对账） |
| 2026-09-19 | W-15 | 反幻觉契约：数字必须绑来源、价格必须带 `as_of` 时点、黑名单域名拒收 | `validate.py::g06_sourcing`；`test_G06_rejects_*`（4 个反例） |
| 2026-09-19 | W-11/W-16 | `generate.py` 五阶段 + `--offline`/`--only`/`--llm-replay`/`--draft-outline`，端到端集成 | `scripts/generate.py`；`tests/test_integration.py::test_pipeline_runs_all_five_stages`（替身跑完 1→5，闸门全绿） |
| 2026-09-19 | W-17 | 咖啡大纲（12 章，覆盖历史/豆种/产地/处理/烘焙/冲煮/特调/品牌/大赛/器具/价格/杯测） | `config/topics/咖啡.json`；`test_seeds_ship_outlines` |
| 2026-09-19 | W-17b | 其余 9 个领域大纲（合计 105 章）与候选红线无关的覆盖面定义 | `config/topics/*.json`；`python -c "outline.load_for_topic(...)"` 10/10 valid |
| 2026-09-19 | W-00 | 需求与环境勘察：读完 `requirement.txt`、读参考项目 `ADEMO/oreilly` 设计语言、确认 `qodercli` v1.13.1 / `copilot` v1.0.84 已登录可用 | `docs/superpowers/specs/...-design.md` §2 spike 表 |
| 2026-09-19 | W-S1 | openspec 装配：`@fission-ai/openspec` v1.13.1 全局安装 + `openspec init --tools qoder --language zh-CN`（spec-driven schema）+ 9 个 openspec skill 收敛到项目作用域 `.qoder/skills/` | `openspec/config.yaml`、`.qoder/commands/opsx/` |
| 2026-09-19 | W-S2 | superpowers 架构设计：调用 `superpowers:brainstorming`，产出设计文档（含 React vs 纯静态 的 A/B/C 方案取舍） | `docs/superpowers/specs/2026-09-19-knowledge-html-generator-design.md` |
| 2026-09-19 | W-S3 | UX design skill 调研与装配：从 skills.sh 检索候选（ux-designer/ui-ux-design-pro 等），最终选定 `anthropics/skills` 官方三件：`frontend-design`（反 AI 默认视觉清单）、`theme-factory`（主题 token）、`web-artifacts-builder`（React/Tailwind 多组件页） | `<用户目录>\.qoder\skills\{frontend-design,theme-factory,web-artifacts-builder}` |
| 2026-09-19 | W-S4 | handover skill 装配：先装 `adobe/skills@handover`，实测判定其为 AEM Edge Delivery 专用（要求 `scripts/aem.js` 存在，否则终止），与需求 5 的"TODO/COMPLETE 台账删减"语义不符 → 卸载并自研项目版 `handover` skill | `.qoder/skills/handover/SKILL.md` |
| 2026-09-19 | W-S5 | R1 风险闭环：验证 qodercli 无头联网 —— 默认拒绝工具，`--permission-mode bypass_permissions` 可拿到真实 URL 与具体数字；同时发现 `web_search_requests` 遥测恒为 0 不可信 | `design.md` §2 + `memo.md` 踩坑节 |
| 2026-09-19 | W-S6 | 图片源可达性勘察：`cn.bing.com/images/async` 可用（12–35 条/查询，murl 原图 + turl 兜底，实测下到真 JPEG/PNG）；Wikimedia、DuckDuckGo 本网络不可达；Baidu acjson 反爬 | `memo.md` 环境事实节 |
